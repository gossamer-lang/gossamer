<!-- generated by `cargo xtask docs-stdlib` -->
# Standard library

This is the Gossamer 0.60.0 standard library reference.
Gossamer's standard library ships as a Rust-implemented host
crate (`gossamer-std`) with a manifest describing every
module and item. This page is auto-generated from that
manifest via `cargo xtask docs-stdlib`; hand edits are
overwritten on the next regeneration.

The manifest itself lives at [`crates/gossamer-std/src/manifest.rs`](https://github.com/gossamer-lang/gossamer/blob/main/crates/gossamer-std/src/manifest.rs).

Names available without any import - the print macros, `min`/`max`/`clamp`, `spawn`, assertions, and the synthesized `from_json::<T>` family - are listed on the [Prelude page](prelude.md).

Every module listed below requires an explicit `use`, such as `use std::env` before calling `env::args()`. Importing one module does not implicitly import sibling modules or its individual functions.

Receiver methods on built-in types such as `String`, `Vec`, `Map`, `Option`, and `Result` are listed in [Methods by type](method_support.md).

## Modules

| Module | Items | Summary |
|--------|------:|---------|
| [`std::archive::tar`](#stdarchivetar) | 3 | Unix tar reader and writer (USTAR / PAX-aware decode). |
| [`std::archive::zip`](#stdarchivezip) | 3 | ZIP archive reader and writer. |
| [`std::bufio`](#stdbufio) | 7 | Buffered readers, writers, and line scanners. |
| [`std::bytes`](#stdbytes) | 5 | Byte buffers, builders, and slice helpers. |
| [`std::collections`](#stdcollections) | 10 | Built-in container types. |
| [`std::compress::bzip2`](#stdcompressbzip2) | 2 | bzip2 encoder / decoder (BZh format). |
| [`std::compress::flate`](#stdcompressflate) | 2 | Raw DEFLATE (RFC 1951) encoder / decoder. |
| [`std::compress::gzip`](#stdcompressgzip) | 3 | gzip encoder / decoder (RFC 1952; flate2-backed). |
| [`std::compress::zlib`](#stdcompresszlib) | 2 | zlib (RFC 1950) encoder / decoder. |
| [`std::compress::zstd`](#stdcompresszstd) | 3 | Zstandard encoder / decoder (RFC 8478; libzstd-vendored). |
| [`std::context`](#stdcontext) | 1 | Request-scoped cancellation, deadlines, and timeouts. |
| [`std::crypto::aead`](#stdcryptoaead) | 4 | Authenticated encryption with associated data. |
| [`std::crypto::blake3`](#stdcryptoblake3) | 2 | BLAKE3 hashing. |
| [`std::crypto::ecdsa`](#stdcryptoecdsa) | 3 | ECDSA over the NIST P-256 curve. |
| [`std::crypto::ed25519`](#stdcryptoed25519) | 3 | Ed25519 digital signatures. |
| [`std::crypto::hmac`](#stdcryptohmac) | 2 | HMAC-SHA-256 keyed MACs. |
| [`std::crypto::insecure`](#stdcryptoinsecure) | 4 | Legacy / broken hashes (MD5, SHA-1). Compat only - never use for new code. |
| [`std::crypto::kdf`](#stdcryptokdf) | 4 | Password-based key-derivation functions. |
| [`std::crypto::password`](#stdcryptopassword) | 3 | Argon2id password hashing facade: PHC-string hash / verify / re-hash policy. |
| [`std::crypto::rand`](#stdcryptorand) | 1 | Secure random bytes from the host CSPRNG. |
| [`std::crypto::sha256`](#stdcryptosha256) | 2 | SHA-256 hashing. |
| [`std::crypto::sha512`](#stdcryptosha512) | 2 | SHA-512 hashing. |
| [`std::crypto::subtle`](#stdcryptosubtle) | 1 | Constant-time comparison helpers. |
| [`std::crypto::x509`](#stdcryptox509) | 3 | X.509 certificate parsing. |
| [`std::database::sql`](#stddatabasesql) | 17 | Driver-pluggable SQL database access. No driver ships in the box; bring your own (Postgres, MySQL, SQLite, ...) by registering one at startup. |
| [`std::encoding::ascii85`](#stdencodingascii85) | 2 | ASCII85 / base85 encode / decode. |
| [`std::encoding::base32`](#stdencodingbase32) | 6 | RFC 4648 base32 (uppercase) encode / decode. |
| [`std::encoding::base64`](#stdencodingbase64) | 2 | RFC 4648 base64 encode/decode. |
| [`std::encoding::binary`](#stdencodingbinary) | 30 | Big/little-endian integer packing and varint codecs. |
| [`std::encoding::csv`](#stdencodingcsv) | 3 | CSV record reader and writer. |
| [`std::encoding::hex`](#stdencodinghex) | 2 | Lowercase hex encode/decode. |
| [`std::encoding::json`](#stdencodingjson) | 21 | JSON parser, emitter, and derive support. |
| [`std::encoding::pem`](#stdencodingpem) | 4 | PEM block encoder and decoder. |
| [`std::encoding::toml`](#stdencodingtoml) | 4 | TOML 1.0 parsing + emission. Pair with the turbofish `from_toml::<Type>` for typed decoding (struct auto-derive). |
| [`std::encoding::xml`](#stdencodingxml) | 6 | Streaming XML decoder + builder (quick-xml). |
| [`std::encoding::yaml`](#stdencodingyaml) | 7 | YAML 1.2 parser/emitter (serde_norway-backed). |
| [`std::env`](#stdenv) | 10 | Process environment, command-line arguments, working directory. |
| [`std::errors`](#stderrors) | 6 | Error construction, wrapping, and chain traversal. |
| [`std::flag`](#stdflag) | 8 | Batteries-included CLI argument parsing. |
| [`std::fmt`](#stdfmt) | 7 | Formatted printing and string interpolation. |
| [`std::fs`](#stdfs) | 35 | Filesystem reading, writing, and traversal (Rust std::fs shape). |
| [`std::hash::adler32`](#stdhashadler32) | 3 | Adler-32 checksums. |
| [`std::hash::crc32`](#stdhashcrc32) | 4 | CRC-32 (IEEE) checksums. |
| [`std::hash::fnv`](#stdhashfnv) | 3 | FNV-1a non-cryptographic hash (32-bit, 64-bit). |
| [`std::html`](#stdhtml) | 2 | HTML text escaping and unescaping. |
| [`std::html::template`](#stdhtmltemplate) | 1 | Context-aware HTML templates with auto-escape (text/attr/URL/JS). The context classifier is heuristic - sound for typical server-rendered responses but NOT a content-security-policy substitute; sanitize untrusted HTML fragments with a dedicated sanitizer. |
| [`std::http`](#stdhttp) | 29 | HTTP/1.1 and HTTP/2 client and server. HTTP/2 negotiates via ALPN over TLS automatically (Go-style); h2c entry points are explicit. Write a handler as a cohort and an arena: `cohort { }` joins or cancels every goroutine the request spawned before the response is written, and its first child failure becomes the block's `Err` for the handler to turn into a status; `arena { }` bump-allocates what the request builds and frees it wholesale on every exit path, with escape checked at compile time. Dependency injection is closure capture - build the router from closures capturing the pool and the configuration. |
| [`std::http::chunked`](#stdhttpchunked) | 4 | RFC 7230 §4.1 chunked transfer-encoding reader and writer. |
| [`std::http::cookie`](#stdhttpcookie) | 5 | RFC 6265 cookie parser and Set-Cookie builder. |
| [`std::http::csrf`](#stdhttpcsrf) | 8 | Double-submit-cookie CSRF protection with Origin / Referer allowlist. |
| [`std::http::form`](#stdhttpform) | 2 | application/x-www-form-urlencoded parser and builder. |
| [`std::http::health`](#stdhttphealth) | 0 | Liveness and readiness endpoints are ordinary handlers over `std::lifecycle`: answer 200 from a liveness route, and 200/503 from `lifecycle::is_ready()` on a readiness route, which drops to false on its own when shutdown begins. A probe registry with per-check timeouts belongs in an application package. |
| [`std::http::middleware`](#stdhttpmiddleware) | 27 | Composable middleware: request_id, cors, security_headers, hsts, cache_control, etag, rate_limit, body_limit, timeout, compress_gzip, logger, recoverer, basic_auth, bearer_auth, safe_defaults. |
| [`std::http::multipart`](#stdhttpmultipart) | 5 | RFC 7578 multipart/form-data streaming parser. |
| [`std::http::native_client`](#stdhttpnative_client) | 6 | Goroutine-driven HTTP/1.1 client over std::net (no ureq, no blocking pool). |
| [`std::http::proxy`](#stdhttpproxy) | 3 | Reverse proxy on top of http::Client. Director-style request mutator + hop-by-hop strip + error handler. |
| [`std::http::query`](#stdhttpquery) | 0 | A request's query string is already parsed: read `request.query` for the raw text and `request.query_pairs` for the decoded name/value pairs. |
| [`std::http::router`](#stdhttprouter) | 6 | Go 1.22-class ServeMux: method-aware path patterns with parameter captures + prefix routes. |
| [`std::http::session`](#stdhttpsession) | 3 | Signs and verifies a session payload. The cookie itself - name, attributes, expiry, a server-side store, id rotation on privilege change, revocation - is application policy and belongs in a session package built on these two. |
| [`std::http::sse`](#stdhttpsse) | 5 | Server-Sent Events (text/event-stream) emitter with heartbeat ticks and retry hint. |
| [`std::http::state`](#stdhttpstate) | 0 | Dependency injection is closure capture: build the router from closures that capture the pool, the cache, and the configuration, and each handler reads what it captured. A captured heap value is shared, so one map serves every request. |
| [`std::http::static_files`](#stdhttpstatic_files) | 3 | Caching static-file handler: ETag, Last-Modified, byte ranges, MIME sniff. |
| [`std::http::websocket`](#stdhttpwebsocket) | 12 | RFC 6455 WebSocket support. Server-side accept + send_text / send_binary / ping / pong / close. |
| [`std::http_h3`](#stdhttp_h3) | 4 | HTTP/3 over QUIC. std::http_h3 is the retained 0.27 spelling; no std::http::h3 alias. |
| [`std::httptest`](#stdhttptest) | 2 | Fixtures for testing HTTP code. A handler is a function from a request to a response, so `record` calls one in memory; a test that is about the wire builds an `http::Server`, binds port 0, and reads the address back. |
| [`std::image`](#stdimage) | 9 | Opaque RGBA8 image handles with PNG and JPEG codecs. |
| [`std::io`](#stdio) | 21 | Stream-oriented I/O abstractions and process standard streams. |
| [`std::iter`](#stditer) | 50 | Sequence adapters: map, filter, fold, zip, enumerate, chain, etc. A `Vec` argument is traversed eagerly; an `Iterator` argument keeps the adapter lazy and answers with another iterator. |
| [`std::jwt`](#stdjwt) | 8 | RFC 7519 tokens. Signs with HS256 / HS384 / HS512, ES256, and EdDSA; verifies those plus the RS256 / RS384 / RS512 family every mainstream identity provider mints with. Claims cross the boundary as JSON text. |
| [`std::lifecycle`](#stdlifecycle) | 7 | Process readiness and graceful shutdown, with systemd sd_notify. Shutdown is observed, not dispatched: wait for it, then drain with ordinary statements - `spawn(|| serve())`, `lifecycle::ready()`, `lifecycle::await_shutdown()`, then the cleanup. |
| [`std::math`](#stdmath) | 46 | Mathematical constants and f64 functions (Go's math package shape). |
| [`std::math::big`](#stdmathbig) | 33 | Arbitrary-precision integers (num-bigint). |
| [`std::math::bits`](#stdmathbits) | 13 | Integer bit-manipulation operations (Go's math/bits shape). |
| [`std::math::rand`](#stdmathrand) | 1 | Deterministic pseudo-random number generation. |
| [`std::metrics`](#stdmetrics) | 6 | Prometheus-compatible primitives (Counter, Gauge, Histogram) and a Registry rendering the standard text-exposition format. |
| [`std::mime`](#stdmime) | 9 | RFC 2045 media type parsing, parameter extraction, and extension lookup. |
| [`std::net`](#stdnet) | 6 | TCP/UDP networking primitives. |
| [`std::net::ip`](#stdnetip) | 10 | String-level IPv4 / IPv6 parsing and classification helpers. |
| [`std::net::netip`](#stdnetnetip) | 11 | Typed IP-address parsing, classification, and addr:port helpers (Go's net/netip shape). |
| [`std::net::smtp`](#stdnetsmtp) | 2 | Sends one message per call, so an application can mail a password reset, an address verification, a magic link, or a security notice. A pool, a queue, retries, and bounce handling are application policy and belong in a package built on these. Port 465 speaks TLS from the first byte; any other port starts in the clear and upgrades through STARTTLS when the server offers it, and credentials are refused rather than sent to a server offering no encryption. |
| [`std::net::url`](#stdneturl) | 5 | Network URL parsing and component escaping; never use filesystem-path rules. |
| [`std::option`](#stdoption) | 16 | Data-last Option combinators for pipeline chaining: map, filter, unwrap_or, and_then, etc. |
| [`std::os`](#stdos) | 2 | Operating-system identity. |
| [`std::os::exec`](#stdosexec) | 12 | Deprecated compatibility facade for child processes; new code uses std::process. |
| [`std::os::signal`](#stdossignal) | 5 | POSIX-style signal subscription (Go's os/signal shape). |
| [`std::os::user`](#stdosuser) | 6 | POSIX user / group lookup. Unix-backed by `nix`; Windows falls back to env vars. |
| [`std::panic`](#stdpanic) | 1 | Panic / `catch_unwind` integration. |
| [`std::path`](#stdpath) | 15 | Lexical filesystem-path operations; platform path grammar, no URL parsing. |
| [`std::pprof`](#stdpprof) | 7 | Runtime profiles in the text format `go tool pprof` reads, plus a Chrome-trace scheduler capture. |
| [`std::process`](#stdprocess) | 13 | Canonical process control and child-process API; std::os::exec is compatibility-only. |
| [`std::regex`](#stdregex) | 11 | Compiled regular expressions (Rust `regex` crate syntax; no backreferences or look-around). |
| [`std::result`](#stdresult) | 10 | Data-last Result combinators for pipeline chaining: map, map_err, unwrap_or_else, etc. |
| [`std::runtime`](#stdruntime) | 13 | Goroutine / scheduler introspection and tuning. |
| [`std::slog`](#stdslog) | 8 | Structured, levelled logging. |
| [`std::sort`](#stdsort) | 3 | Explicit stable ordering and sorted-sequence search, the deliberate counterpart to Vec's unstable inherent `sort`. |
| [`std::strconv`](#stdstrconv) | 10 | Conversions between strings and primitive numeric types. |
| [`std::strings`](#stdstrings) | 43 | String operations. |
| [`std::sync`](#stdsync) | 16 | Synchronisation primitives beyond channels. |
| [`std::testing`](#stdtesting) | 5 | Assertions and sub-test harness helpers. |
| [`std::thread`](#stdthread) | 2 | OS-thread scheduling hints and CPU introspection; user concurrency uses goroutines, not thread spawning. |
| [`std::time`](#stdtime) | 24 | Wall-clock and monotonic time facilities. |
| [`std::tls`](#stdtls) | 0 | Rustls-backed TLS support exposed through `http::serve_tls` and `net::TcpStream` TLS upgrades. The configuration constructors are host-runtime internals, not Gossamer callables. |
| [`std::trace`](#stdtrace) | 8 | W3C trace-context-compatible distributed tracing. Identifier types, request-scoped SpanContext, process-level Tracer, and OTLP JSON export. |
| [`std::unicode`](#stdunicode) | 37 | Unicode general-category predicates, casing, normalization, and segmentation. |
| [`std::utf16`](#stdutf16) | 5 | UTF-16 encoding/decoding and surrogate pair helpers. |
| [`std::utf8`](#stdutf8) | 14 | UTF-8 validation and scalar decoding. |
| [`std::uuid`](#stduuid) | 5 | UUID v4 (random) and v7 (timestamp-ordered) generation, parse, and normalize. |
| [`std::validate`](#stdvalidate) | 3 | Trait-based field validation: implement Validate, collect FieldErrors into Errors. |

## `std::archive::tar`

Unix tar reader and writer (USTAR / PAX-aware decode).

| Item | Kind | Doc |
|------|------|-----|
| `TarEntry` | type | name + data + is_dir flag. |
| `read` | fn | Reads all entries from a tar archive. |
| `write` | fn | Builds a tar archive from (name, data) pairs. |

## `std::archive::zip`

ZIP archive reader and writer.

| Item | Kind | Doc |
|------|------|-----|
| `ZipEntry` | type | name + decompressed data + is_dir flag. |
| `read` | fn | Reads all file entries from a zip stored in `data`. |
| `write` | fn | Builds an in-memory zip from (name, data) pairs. |

## `std::bufio`

Buffered readers, writers, and line scanners.

| Item | Kind | Doc |
|------|------|-----|
| `Reader` | type | Buffered reader. |
| `Scanner` | type | Line / token scanner. |
| `Writer` | type | Buffered writer. |
| `read_lines` | fn | Reads every line from a file path; one-shot convenience over the streaming Scanner. |
| `read_lines_of` | fn | Reads every line of a file path into a Vec<String>. |
| `read_to_string` | fn | Reads an entire file path into a String. |
| `split_whitespace` | fn | Splits a String on runs of whitespace. |

## `std::bytes`

Byte buffers, builders, and slice helpers.

| Item | Kind | Doc |
|------|------|-----|
| `Buffer` | type | Growable byte buffer for incremental assembly: new, with_capacity, write_str, push, len, is_empty, clear, to_string. A buffer you index, slice, or edit at an offset is a Vec<u8>. |
| `Builder` | type | Incremental string builder: new, write, write_char, len, build, as_str. Cheaper than repeated `+` on a String, which copies. |
| `index_of` | fn | First occurrence of a byte needle. |
| `replace` | fn | Replaces every occurrence of a byte needle. |
| `split` | fn | Splits on every separator occurrence. |

## `std::collections`

Built-in container types.

| Item | Kind | Doc |
|------|------|-----|
| `BTreeMap` | type | Ordered key-value map backed by BTreeMap. |
| `BTreeSet` | type | Ordered unique-value set backed by BTreeSet. |
| `Deque` | type | Double-ended queue with explicit front/back methods; use `Deque::new()` or `Deque::from([a, b])`. |
| `Map` | type | Key-value map backed by the swiss-table layout; literals use `{key: value}` and empty map literals use `{}`. |
| `MaxHeap` | type | Max-priority heap; use `MaxHeap::new()` or `MaxHeap::from([a, b])`. |
| `MinHeap` | type | Min-priority heap; use `MinHeap::new()` or `MinHeap::from([a, b])`. |
| `Queue` | type | FIFO-only queue; use `Queue::new()` or `Queue::from([a, b])`. |
| `Set` | type | Unique-value set backed by a hash table; literals use `#{a, b}` and empty set literals use `#{}`. |
| `Stack` | type | LIFO-only stack; use `Stack::new()` or `Stack::from([a, b])`. |
| `Vec` | type | Growable contiguous sequence. Vec literals use `[a, b]` and empty Vec literals use `[]`. |

## `std::compress::bzip2`

bzip2 encoder / decoder (BZh format).

| Item | Kind | Doc |
|------|------|-----|
| `compress` | fn | One-shot bzip2 compress. |
| `decompress` | fn | One-shot bzip2 decompress. |

## `std::compress::flate`

Raw DEFLATE (RFC 1951) encoder / decoder.

| Item | Kind | Doc |
|------|------|-----|
| `compress` | fn | One-shot DEFLATE compress. |
| `decompress` | fn | One-shot DEFLATE decompress. |

## `std::compress::gzip`

gzip encoder / decoder (RFC 1952; flate2-backed).

| Item | Kind | Doc |
|------|------|-----|
| `Level` | type | Compression level (`0` store-only … `9` best); default is gzip(1)'s `6`. |
| `decode` | fn | Decompresses a gzip-formatted payload. |
| `encode` | fn | Compresses bytes at the supplied Level. |

## `std::compress::zlib`

zlib (RFC 1950) encoder / decoder.

| Item | Kind | Doc |
|------|------|-----|
| `compress` | fn | One-shot zlib compress. |
| `decompress` | fn | One-shot zlib decompress. |

## `std::compress::zstd`

Zstandard encoder / decoder (RFC 8478; libzstd-vendored).

| Item | Kind | Doc |
|------|------|-----|
| `decode` | fn | One-shot Zstandard decompress. |
| `encode` | fn | One-shot Zstandard compress at the default level (3). |
| `encode_level` | fn | One-shot Zstandard compress at the supplied level (1 fastest -- 22 best). |

## `std::context`

Request-scoped cancellation, deadlines, and timeouts.

| Item | Kind | Doc |
|------|------|-----|
| `Context` | type | Cancellation-aware context handle. |

## `std::crypto::aead`

Authenticated encryption with associated data.

| Item | Kind | Doc |
|------|------|-----|
| `aes_256_gcm_open` | fn | AES-256-GCM open: decrypts and authenticates ciphertext. |
| `aes_256_gcm_seal` | fn | AES-256-GCM seal: encrypts plaintext with key, nonce, and AAD. |
| `chacha20_poly1305_open` | fn | ChaCha20-Poly1305 open. |
| `chacha20_poly1305_seal` | fn | ChaCha20-Poly1305 seal. |

## `std::crypto::blake3`

BLAKE3 hashing.

| Item | Kind | Doc |
|------|------|-----|
| `digest` | fn | Returns the 32-byte BLAKE3 digest of an input. |
| `hex` | fn | Returns the digest as lowercase hex. |

## `std::crypto::ecdsa`

ECDSA over the NIST P-256 curve.

| Item | Kind | Doc |
|------|------|-----|
| `keypair_pem` | fn | Generates (secret_pem, public_pem) for a fresh P-256 keypair. |
| `sign_pem` | fn | Signs a message with a PKCS#8-PEM-encoded P-256 secret key. |
| `verify_pem` | fn | Verifies a DER-encoded signature against an SPKI-PEM public key. |

## `std::crypto::ed25519`

Ed25519 digital signatures.

| Item | Kind | Doc |
|------|------|-----|
| `keypair` | fn | Generates a fresh Ed25519 keypair from the host CSPRNG. |
| `sign` | fn | Signs a message with a 32-byte secret key. |
| `verify` | fn | Verifies a 64-byte signature against a 32-byte public key. |

## `std::crypto::hmac`

HMAC-SHA-256 keyed MACs.

| Item | Kind | Doc |
|------|------|-----|
| `sha256_hex` | fn | HMAC-SHA-256 over a message, hex-encoded. |
| `sha256_mac` | fn | HMAC-SHA-256 over a message. |

## `std::crypto::insecure`

Legacy / broken hashes (MD5, SHA-1). Compat only - never use for new code.

| Item | Kind | Doc |
|------|------|-----|
| `md5` | fn | One-shot MD5. |
| `md5_hex` | fn | One-shot MD5, hex-encoded. |
| `sha1` | fn | One-shot SHA-1. |
| `sha1_hex` | fn | One-shot SHA-1, hex-encoded. |

## `std::crypto::kdf`

Password-based key-derivation functions.

| Item | Kind | Doc |
|------|------|-----|
| `argon2id_hash` | fn | Argon2id PHC-format password hash. |
| `argon2id_verify` | fn | Verifies a password against an Argon2id PHC string. |
| `pbkdf2_sha256` | fn | PBKDF2-HMAC-SHA256 KDF. |
| `scrypt_interactive` | fn | scrypt with the standard interactive parameters. |

## `std::crypto::password`

Argon2id password hashing facade: PHC-string hash / verify / re-hash policy.

| Item | Kind | Doc |
|------|------|-----|
| `hash` | fn | Argon2id hash of plaintext; returns a PHC-format string for storage. |
| `needs_rehash` | fn | True iff the stored PHC's parameters are below the current defaults. |
| `verify` | fn | Constant-time verify of plaintext against a stored PHC string. |

## `std::crypto::rand`

Secure random bytes from the host CSPRNG.

| Item | Kind | Doc |
|------|------|-----|
| `bytes` | fn | Returns a fresh random byte vector. |

## `std::crypto::sha256`

SHA-256 hashing.

| Item | Kind | Doc |
|------|------|-----|
| `digest` | fn | Returns the 32-byte digest of an input. |
| `hex` | fn | Returns the digest as lowercase hex. |

## `std::crypto::sha512`

SHA-512 hashing.

| Item | Kind | Doc |
|------|------|-----|
| `digest` | fn | Returns the 64-byte digest of an input. |
| `hex` | fn | Returns the digest as lowercase hex. |

## `std::crypto::subtle`

Constant-time comparison helpers.

| Item | Kind | Doc |
|------|------|-----|
| `constant_time_eq` | fn | Compares two byte slices without data-dependent branches. |

## `std::crypto::x509`

X.509 certificate parsing.

| Item | Kind | Doc |
|------|------|-----|
| `CertInfo` | type | Inspected fields of an X.509 certificate. |
| `parse_pem` | fn | Parses one PEM-encoded certificate. |
| `verify_server_certificate_with_crls` | fn | Fail-closed TLS server-chain, hostname, and CRL verification. |

## `std::database::sql`

Driver-pluggable SQL database access. No driver ships in the box; bring your own (Postgres, MySQL, SQLite, ...) by registering one at startup.

| Item | Kind | Doc |
|------|------|-----|
| `Conn` | type | Open database connection. `prepare`, `execute`, `query`, `query_each`, `begin`, `begin_with`, `ping`, `execute_many`, `execute_ctx`, `query_ctx`, `interrupt`, `close` (closing sweeps any cursors still open on the connection). |
| `Error` | type | Driver error. `Error::driver(driver, msg)` builds one; `Error::PoolExhausted` and `Error::Cancelled` are variants. |
| `IsolationLevel` | type | Default / ReadUncommitted / ReadCommitted / RepeatableRead / Serializable. Passed to `Conn::begin_with`. |
| `Pool` | type | Connection pool. `new`, `fill`, `get` (blocks up to `acquire_timeout`), `len`. Cheap to clone. |
| `PoolConfig` | type | Pool tuning: `min`, `max`, `idle_timeout`, `max_lifetime`, `acquire_timeout`, `statement_cache`. Fluent `with_*` builders. |
| `PooledConn` | type | Connection checked out from a `Pool`; returned on drop. |
| `Row` | type | Current row inside a `Rows` walk; valid until the cursor advances or closes. Typed `get_i64`, `get_f64`, `get_bool`, `get_text`, `get_blob` plus `get_opt_*` and `is_null`. |
| `Rows` | type | Result-set cursor. `next_row`, `columns`, `close` (idempotent). Advancing frees the previous Row; a full drain reclaims everything. For early exits, `defer rows.close()`. |
| `Select` | type | Fluent SELECT builder. `Select::new(table).columns([..]).where_eq(col, sql::Value::Int(...))...render() -> String`; `.params()` returns the bound parameters. Emits Postgres-style `$N` placeholders. |
| `Stmt` | type | Prepared statement. |
| `Tx` | type | Active transaction. `commit`, `rollback`, `savepoint`, `release_savepoint`, `rollback_to_savepoint`, `execute`. |
| `Value` | type | Bound or fetched value. Null / Bool / Int / Float / Text / Blob. |
| `Driver` | trait | Host-side database driver contract. Rust bindings register implementations before the Gossamer program starts. |
| `drivers` | fn | Lists every currently-registered driver name. |
| `migrate_up` | fn | Applies pending forward-only schema migrations from a directory of `<version>_<slug>.sql` files. `migrate::up(&mut conn, dir)` is an equivalent namespaced spelling. |
| `open` | fn | Opens a database connection by driver name + URL. |
| `register_native` | fn | Registers a Gossamer-native driver under its canonical name. The driver must provide the SQL dispatch surface used by the runtime. |

## `std::encoding::ascii85`

ASCII85 / base85 encode / decode.

| Item | Kind | Doc |
|------|------|-----|
| `decode` | fn | ASCII85 string -> bytes. |
| `encode` | fn | Bytes -> ASCII85 string. |

## `std::encoding::base32`

RFC 4648 base32 (uppercase) encode / decode.

| Item | Kind | Doc |
|------|------|-----|
| `decode` | fn | Base32 string -> bytes. |
| `decode_hex` | fn | Decodes extended-hex base32 text into a String. |
| `decode_string` | fn | Decodes standard base32 text into a String. |
| `encode` | fn | Bytes -> base32 string. |
| `encode_hex` | fn | Encodes a String as extended-hex base32 text. |
| `encode_string` | fn | Encodes a String as standard base32 text. |

## `std::encoding::base64`

RFC 4648 base64 encode/decode.

| Item | Kind | Doc |
|------|------|-----|
| `decode` | fn | Decodes a base64 string. |
| `encode` | fn | Encodes bytes to a base64 string. |

## `std::encoding::binary`

Big/little-endian integer packing and varint codecs.

| Item | Kind | Doc |
|------|------|-----|
| `get_u16_be` | fn | Reads a big-endian u16. |
| `get_u16_be_at` | fn | Reads a big-endian u16 at a byte offset of an existing buffer. An offset plus width past the end is an Err, never a zero-fill. |
| `get_u16_le` | fn | Reads a little-endian u16. |
| `get_u16_le_at` | fn | Reads a little-endian u16 at a byte offset of an existing buffer. An offset plus width past the end is an Err, never a zero-fill. |
| `get_u32_be` | fn | Reads a big-endian u32. |
| `get_u32_be_at` | fn | Reads a big-endian u32 at a byte offset of an existing buffer. An offset plus width past the end is an Err, never a zero-fill. |
| `get_u32_le` | fn | Reads a little-endian u32. |
| `get_u32_le_at` | fn | Reads a little-endian u32 at a byte offset of an existing buffer. An offset plus width past the end is an Err, never a zero-fill. |
| `get_u64_be` | fn | Reads a big-endian u64. |
| `get_u64_be_at` | fn | Reads a big-endian u64 at a byte offset of an existing buffer. An offset plus width past the end is an Err, never a zero-fill. |
| `get_u64_le` | fn | Reads a little-endian u64. |
| `get_u64_le_at` | fn | Reads a little-endian u64 at a byte offset of an existing buffer. An offset plus width past the end is an Err, never a zero-fill. |
| `get_u8` | fn | Reads a single byte. |
| `put_u16_be` | fn | Writes a big-endian u16. |
| `put_u16_be_at` | fn | Writes a big-endian u16 at a byte offset, in place through the caller's buffer. An offset plus width past the end is an Err. |
| `put_u16_le` | fn | Writes a little-endian u16. |
| `put_u16_le_at` | fn | Writes a little-endian u16 at a byte offset, in place through the caller's buffer. An offset plus width past the end is an Err. |
| `put_u32_be` | fn | Writes a big-endian u32. |
| `put_u32_be_at` | fn | Writes a big-endian u32 at a byte offset, in place through the caller's buffer. An offset plus width past the end is an Err. |
| `put_u32_le` | fn | Writes a little-endian u32. |
| `put_u32_le_at` | fn | Writes a little-endian u32 at a byte offset, in place through the caller's buffer. An offset plus width past the end is an Err. |
| `put_u64_be` | fn | Writes a big-endian u64. |
| `put_u64_be_at` | fn | Writes a big-endian u64 at a byte offset, in place through the caller's buffer. An offset plus width past the end is an Err. |
| `put_u64_le` | fn | Writes a little-endian u64. |
| `put_u64_le_at` | fn | Writes a little-endian u64 at a byte offset, in place through the caller's buffer. An offset plus width past the end is an Err. |
| `put_u8` | fn | Writes a single byte. |
| `put_uvarint` | fn | Encodes an unsigned varint. |
| `put_varint` | fn | Encodes a signed varint (zigzag). |
| `uvarint` | fn | Decodes an unsigned varint. |
| `varint` | fn | Decodes a signed varint (zigzag). |

## `std::encoding::csv`

CSV record reader and writer.

| Item | Kind | Doc |
|------|------|-----|
| `parse_line` | fn | Parses a single CSV-formatted line. |
| `read` | fn | Parses all CSV records from a string. |
| `write` | fn | Serialises records as a CSV string. |

## `std::encoding::hex`

Lowercase hex encode/decode.

| Item | Kind | Doc |
|------|------|-----|
| `decode` | fn | Decodes a hex string. |
| `encode` | fn | Encodes bytes to hex. |

## `std::encoding::json`

JSON parser, emitter, and derive support.

| Item | Kind | Doc |
|------|------|-----|
| `Error` | type | Error raised by encoding/decoding operations. |
| `Value` | type | Dynamically typed JSON value. |
| `Deserialize` | trait | Trait for parsing a value from JSON. |
| `Serialize` | trait | Trait for converting a value to JSON. |
| `as_array` | fn | Reads a dynamic Value as an array of Values. |
| `as_bool` | fn | Reads a dynamic Value as Option<bool>. |
| `as_f64` | fn | Reads a dynamic Value as Option<f64>. |
| `as_i64` | fn | Reads a dynamic Value as Option<i64>. |
| `as_str` | fn | Reads a dynamic Value as Option<String>. |
| `at` | fn | Indexes an array element on a dynamic Value. An index the array does not have reads as JSON null. |
| `decode` | fn | Decodes a JSON `String` into a `Deserialize` value. |
| `encode` | fn | Encodes a `Serialize` value as a JSON `String`. |
| `encode_pretty` | fn | Renders a value as indented JSON text. |
| `get` | fn | Looks up an object field on a dynamic Value. |
| `is_null` | fn | Reports whether a dynamic Value is null. |
| `keys` | fn | Object field names of a dynamic Value. |
| `len` | fn | Element / field count of a dynamic Value. |
| `parse` | fn | Parses JSON text into a dynamic Value. |
| `render` | fn | Renders a dynamic Value as compact JSON text. |
| `set` | fn | Sets an object field on a dynamic Value. |
| `valid` | fn | Reports whether the text is well-formed JSON. |

## `std::encoding::pem`

PEM block encoder and decoder.

| Item | Kind | Doc |
|------|------|-----|
| `Block` | type | A decoded PEM block with type label and DER bytes. |
| `decode` | fn | Decodes the first PEM block from a string. |
| `decode_all` | fn | Decodes all PEM blocks from a string. |
| `encode` | fn | Encodes a Block as a PEM string. |

## `std::encoding::toml`

TOML 1.0 parsing + emission. Pair with the turbofish `from_toml::<Type>` for typed decoding (struct auto-derive).

| Item | Kind | Doc |
|------|------|-----|
| `from_json` | fn | Render JSON text as TOML text; returns Result<String, errors::Error>. |
| `is_valid` | fn | Return true iff the string parses as TOML. |
| `pretty` | fn | Round-trip TOML through the pretty-printer. |
| `to_json` | fn | Convert TOML text to JSON text; returns Result<String, errors::Error>. |

## `std::encoding::xml`

Streaming XML decoder + builder (quick-xml).

| Item | Kind | Doc |
|------|------|-----|
| `Event` | type | Start / End / Text / CData / Comment / Eof. |
| `Reader` | type | Pull-style XML reader. |
| `Writer` | type | Streaming XML writer. |
| `encode` | fn | Serialises a sequence of events to XML text. |
| `escape` | fn | Escapes XML metacharacters in text. |
| `parse` | fn | Parses an XML document into a Vec of events. |

## `std::encoding::yaml`

YAML 1.2 parser/emitter (serde_norway-backed).

| Item | Kind | Doc |
|------|------|-----|
| `Value` | type | Dynamically typed YAML value. |
| `encode` | fn | Encodes a Value as a YAML document. |
| `from_json` | fn | Converts JSON text to a YAML document. |
| `is_valid` | fn | Reports whether the text is well-formed YAML. |
| `parse` | fn | Parses a YAML document into a Value. |
| `parse_all` | fn | Parses a multi-document YAML stream into a Vec<Value>. |
| `to_json` | fn | Converts a YAML document to JSON text. |

## `std::env`

Process environment, command-line arguments, working directory.

| Item | Kind | Doc |
|------|------|-----|
| `args` | fn | Returns the program's command-line arguments. |
| `current_dir` | fn | Returns the current working directory. |
| `home_dir` | fn | Returns the calling user's home directory if known. |
| `program_name` | fn | Returns the path used to invoke the program (argv[0]). |
| `set_current_dir` | fn | Changes the current working directory. |
| `set_var` | fn | Sets an environment variable in the current process. |
| `temp_dir` | fn | Returns the system's temporary directory. |
| `unset_var` | fn | Removes an environment variable from the current process. |
| `var` | fn | Returns the value of an environment variable. |
| `vars` | fn | vars() -> Map<String, String>. Every environment variable this process has, as a snapshot. |

## `std::errors`

Error construction, wrapping, and chain traversal.

| Item | Kind | Doc |
|------|------|-----|
| `Error` | type | Error value carrying a message, an optional cause, and structured diagnostic fields. Methods: `message() -> String` (top message only), `cause() -> Option<Error>`, `chain() -> Vec<Error>` (self then every ancestor cause), `is(needle) -> bool`, `with_field(key, value) -> Error` (an immutable copy carrying one more field; re-setting a key replaces its value), `field(key) -> Option<String>`, `fields() -> Vec<(String, String)>` in insertion order. `{}` renders the colon-joined chain. Example: `let e = errors::new("query failed").with_field("sqlstate", "23505")`. |
| `is` | fn | `is(error, needle) -> bool` - true when `needle` matches `error` or any link of its cause chain. Prefer a sentinel error VALUE, which matches by identity so two errors sharing a message stay distinct: `let NOT_FOUND = errors::new("not found")`, then `errors::is(err, NOT_FOUND)`. A String `needle` falls back to a message substring test. |
| `join` | fn | Joins a list of errors into one; messages are joined with "; " (None for an empty list). |
| `new` | fn | Constructs a fresh error from a message. |
| `newf` | fn | Constructs a fresh error from a format template, e.g. `newf("status {}", code)`. |
| `wrap` | fn | Wraps a cause with a higher-level message. |

## `std::flag`

Batteries-included CLI argument parsing.

| Item | Kind | Doc |
|------|------|-----|
| `Error` | type | Error produced while parsing flags. |
| `Set` | type | Flag definition + parsing set. |
| `Value` | type | Parsed command-line flag value. |
| `bool` | fn | Defines a boolean flag on the default set. |
| `define` | fn | Registers a flag definition on the default set. |
| `int` | fn | Defines an integer flag on the default set. |
| `parse` | fn | Parses the default flag set against the given args. |
| `string` | fn | Defines a string flag on the default set. |

## `std::fmt`

Formatted printing and string interpolation.

| Item | Kind | Doc |
|------|------|-----|
| `Debug` | trait | How a value renders through `{:?}`. The rendering is synthesized; `impl Debug for T { fn fmt(&self) -> String }` overrides it. |
| `Display` | trait | How a value renders through `{}`. The rendering is synthesized; `impl Display for T { fn fmt(&self) -> String }` overrides it, and `x.to_string()` reaches the same rendering. |
| `eprint` | builtin | Prints to stderr without a trailing newline. |
| `eprintln` | builtin | Prints to stderr followed by a newline. |
| `format` | builtin | Formats arguments into an owned `String`. |
| `print` | builtin | Prints to stdout without a trailing newline. |
| `println` | builtin | Prints to stdout followed by a newline. |

## `std::fs`

Filesystem reading, writing, and traversal (Rust std::fs shape).

| Item | Kind | Doc |
|------|------|-----|
| `DirInfo` | type | Directory entry yielded by read_dir and walk_dir; carries path, name, is_file, is_dir, is_symlink, and size. |
| `File` | type | Streaming file handle. Reads and writes at the handle's own cursor (read, read_to_string, write, write_bytes, seek), positionally (read_at, write_at), and reports size (len, set_len). Durability is sync_all / sync_data; multi-process safety is the try_lock_* / unlock family. |
| `OpenOptions` | type | Builder for opening files with read/write/append/create/truncate flags. |
| `canonicalize` | fn | Resolves a path to an absolute, symlink-free canonical form. |
| `copy` | fn | Copies a file, creating parent dirs as needed. |
| `create` | fn | Creates or truncates a file and returns a streaming file handle. |
| `create_dir` | fn | Creates a single directory. Fails if any parent is missing. |
| `create_dir_all` | fn | Creates a directory and any missing ancestors. |
| `create_dir_all_mode` | fn | Creates a directory and any missing ancestors, giving each one it creates exactly this mode. |
| `create_dir_mode` | fn | Creates a single directory with exactly this mode, whatever the umask is. On Windows only the owner write bit is meaningful: it sets the read-only attribute. |
| `exists` | fn | Returns whether a path exists on the filesystem. |
| `file_size` | fn | Returns the file's size in bytes; 0 on error. |
| `is_dir` | fn | Returns whether a path exists and is a directory. |
| `is_file` | fn | Returns whether a path exists and is a regular file. |
| `is_symlink` | fn | Returns whether a path exists and is a symbolic link. |
| `metadata` | fn | Returns filesystem metadata for a path. |
| `open` | fn | Opens a file for streaming reads. |
| `permissions` | fn | The permission bits of a path, in the chmod(2) encoding. On Windows the read-only attribute is widened into the bits an equivalent Unix path would carry. |
| `read` | fn | Reads an entire file into memory as bytes. |
| `read_dir` | fn | Returns immediate children as DirInfo values. Inspect their metadata fields directly; each path can be passed back to filesystem APIs. |
| `read_to_string` | fn | Reads an entire file into memory as UTF-8 text. |
| `remove_dir` | fn | Removes an empty directory. |
| `remove_dir_all` | fn | Recursively removes a directory and its contents. |
| `remove_file` | fn | Removes a single file. |
| `rename` | fn | Renames a file or directory. |
| `set_permissions` | fn | Sets the permission bits of a path, in the chmod(2) encoding. On Windows only the owner write bit is meaningful: it sets or clears the read-only attribute. |
| `sync_dir` | fn | Makes a directory's own entries durable - the barrier a create, rename, or delete needs after the file itself is synced. On Windows this is satisfied by NTFS metadata ordering and performs no flush. |
| `temp_dir` | fn | Creates a unique temporary directory; the caller removes it explicitly. |
| `temp_file` | fn | Creates a unique temporary file and returns its handle plus path. |
| `walk_dir` | fn | Recursively visits every descendant entry. |
| `write` | fn | Writes bytes to a file, creating or truncating it. |
| `write_mode` | fn | Writes a file and leaves it at exactly this mode, whatever the umask is. |
| `SEEK_CUR` | const | File::seek whence: the offset is relative to the current position. |
| `SEEK_END` | const | File::seek whence: the offset is relative to the end of the file. |
| `SEEK_SET` | const | File::seek whence: the offset is absolute from the start of the file. |

## `std::hash::adler32`

Adler-32 checksums.

| Item | Kind | Doc |
|------|------|-----|
| `checksum` | fn | Adler-32 checksum of a byte slice. |
| `checksum_string` | fn | Adler-32 checksum of a String. |
| `update` | fn | Continues an Adler-32 from a running value over more bytes. |

## `std::hash::crc32`

CRC-32 (IEEE) checksums.

| Item | Kind | Doc |
|------|------|-----|
| `checksum` | fn | CRC-32 checksum of a byte slice. |
| `checksum_string` | fn | CRC-32 checksum of a String. |
| `update` | fn | Continues a CRC-32 from a running value over more bytes. |
| `update_window` | fn | Continues a CRC-32 over `data[start..end]`, checking a record where it lies rather than copying the window out first. |

## `std::hash::fnv`

FNV-1a non-cryptographic hash (32-bit, 64-bit).

| Item | Kind | Doc |
|------|------|-----|
| `hash32` | fn | 32-bit FNV-1a of a byte slice. |
| `hash64` | fn | 64-bit FNV-1a of a byte slice. |
| `hash_string` | fn | 64-bit FNV-1a of a String. |

## `std::html`

HTML text escaping and unescaping.

| Item | Kind | Doc |
|------|------|-----|
| `escape` | fn | Escapes HTML metacharacters (, <, >, ", '). |
| `unescape` | fn | Resolves HTML entities back to their characters. |

## `std::html::template`

Context-aware HTML templates with auto-escape (text/attr/URL/JS). The context classifier is heuristic - sound for typical server-rendered responses but NOT a content-security-policy substitute; sanitize untrusted HTML fragments with a dedicated sanitizer.

| Item | Kind | Doc |
|------|------|-----|
| `render_json` | fn | render_json(source, json_data) -> Result<String, Error>: renders a context-aware HTML template against a JSON data context. Stateless and wired bit-identically across every tier. |

## `std::http`

HTTP/1.1 and HTTP/2 client and server. HTTP/2 negotiates via ALPN over TLS automatically (Go-style); h2c entry points are explicit. Write a handler as a cohort and an arena: `cohort { }` joins or cancels every goroutine the request spawned before the response is written, and its first child failure becomes the block's `Err` for the handler to turn into a status; `arena { }` bump-allocates what the request builds and frees it wholesale on every exit path, with escape checked at compile time. Dependency injection is closure capture - build the router from closures capturing the pool and the configuration.

| Item | Kind | Doc |
|------|------|-----|
| `Client` | type | HTTP client; configure redirects and timeout via `Client::builder()`. |
| `Headers` | type | Case-insensitive header map. |
| `Http2Config` | type | Per-connection HTTP/2 tuning (window sizes, max concurrent streams, frame caps). |
| `Http2Error` | type | HTTP/2 server error: Io, Protocol, Handler. |
| `Http2ServerHandle` | type | Handle to a running HTTP/2 connection for shutdown / in-flight counts. |
| `Method` | type | HTTP method enumeration. |
| `PushOptions` | type | Prioritization knobs for `ResponseWriter::push_promise` (weight, depends_on, exclusive). |
| `PushStream` | type | Server-initiated push stream returned by `ResponseWriter::push_promise`. Supports send_head / write / write_trailers / end. |
| `Request` | type | HTTP request value passed to a handler. Fields: `method`, `path`, `query`, `query_pairs`, `headers`, `body`, `raw_body`, `peer_addr`. |
| `Response` | type | HTTP response value returned from a handler. |
| `ResponseStream` | type | A streaming response body. `http::stream` produces one over an upstream response, read with `next_line` / `next_chunk`; `ResponseStream::new()` opens one the handler writes itself, with `write(text)` and `write_bytes(bytes)` answering the queued byte count (or -1 once closed), `is_open()` saying whether the client is still there, and `close()` ending the body. Either kind is consumed by `Response::stream(status, content_type, body)` - write from a goroutine and the handler answers immediately while the bytes are framed as they arrive. |
| `Server` | type | A configured server. `http::serve(addr, handler)` keeps every default; build a `Server` when a deployment needs its own budgets, its bound address read back, or a shutdown it drives. Setters chain and answer the server: `read_header_timeout_ms`, `read_body_timeout_ms`, `write_timeout_ms`, `idle_timeout_ms`, `request_timeout_ms`, `max_header_bytes`, `max_body_bytes`, `max_connections`, `server_name`. Then `listen(addr) -> Result<(), Error>` binds - so `addr() -> String` reads back a port-0 assignment before `serve(handler)` blocks - and `shutdown(deadline_ms) -> bool` stops accepting and waits for in-flight requests, answering whether the drain finished. |
| `StatusCode` | type | HTTP status code. |
| `StreamingResponseWriter` | type | Streaming HTTP/2 response writer; set_status / header / write_chunk / finish. |
| `Trailers` | type | HTTP/2 trailing HEADERS (alias for Headers) - used by `ResponseWriter::write_trailers` and `Request::trailers`. |
| `Http2Handler` | trait | Bounded-body HTTP/2 handler: serve(Request) -> Response. |
| `Http2StreamingHandler` | trait | Chunked-body HTTP/2 handler: serve(Request, StreamingResponseWriter) -> Result. |
| `delete` | fn | One-shot DELETE: `(url, body, headers) -> Result<Response, Error>`. |
| `get` | fn | One-shot GET: `(url, headers) -> Result<Response, Error>`. |
| `head` | fn | One-shot HEAD: `(url, headers) -> Result<Response, Error>`. |
| `options` | fn | One-shot OPTIONS: `(url, headers) -> Result<Response, Error>`. |
| `post` | fn | One-shot POST: `(url, body, content_type) -> Result<Response, Error>`. |
| `put` | fn | One-shot PUT: `(url, body, content_type) -> Result<Response, Error>`. |
| `request` | fn | One-shot request with a string body: `(method, url, body, headers) -> Result<Response, Error>`. |
| `request_bytes` | fn | One-shot request with a byte body: `(method, url, body: [u8], headers) -> Result<Response, Error>`. |
| `serve` | fn | Convenience: bind and serve an HTTP handler. `Result<(), Error>` - a bind failure is an Err value. |
| `serve_h2c` | fn | Bind a plain-TCP listener and serve h2c (HTTP/2 cleartext). |
| `serve_tls` | fn | TLS-terminating server: `serve_tls(addr, cert_pem, key_pem, handler) -> Result<(), Error>`. Builds a rustls config from the PEM cert chain + key and serves HTTPS with the same handler contract as `serve`. |
| `stream` | fn | One-shot request read incrementally: `(method, url, body, headers) -> Result<ResponseStream, Error>`. |

## `std::http::chunked`

RFC 7230 §4.1 chunked transfer-encoding reader and writer.

| Item | Kind | Doc |
|------|------|-----|
| `Reader` | type | Decodes a chunked body from any Read source (Rust-side; streaming). |
| `Writer` | type | Encodes raw bytes into chunked frames over any Write sink (Rust-side; streaming). |
| `decode` | fn | One-shot: concatenates data chunks from a complete chunked body. Available in interp + compiled. |
| `encode` | fn | One-shot: wraps a buffer in chunked transfer-encoding with terminator. Available in interp + compiled. |

## `std::http::cookie`

RFC 6265 cookie parser and Set-Cookie builder.

| Item | Kind | Doc |
|------|------|-----|
| `Cookie` | type | Parsed cookie with name, value, and Set-Cookie attributes. |
| `CookieBuilder` | type | Fluent builder for Set-Cookie response headers. |
| `SameSite` | type | SameSite attribute: Strict / Lax / None. |
| `parse_cookie_header` | fn | Parse a Cookie request header into (name, value) pairs. |
| `serialize` | fn | Render a Cookie as a Set-Cookie header value. |

## `std::http::csrf`

Double-submit-cookie CSRF protection with Origin / Referer allowlist.

| Item | Kind | Doc |
|------|------|-----|
| `Config` | type | Signing key, cookie / header names, and origin allowlist. |
| `RouteAuth` | type | Per-route policy: Required, Optional, or Skipped. |
| `attach_cookie` | fn | Set the CSRF cookie on a Response. |
| `check` | fn | Combined origin + token gate; returns Err on failure. |
| `extract_token` | fn | Pull a token from the configured header or form field. |
| `issue_token` | fn | Mint a fresh CSRF token bound to the configured signing key. |
| `origin_allowed` | fn | Origin / Referer allowlist check for unsafe methods. |
| `verify_token` | fn | Constant-time verify of a presented token against the cookie value. |

## `std::http::form`

application/x-www-form-urlencoded parser and builder.

| Item | Kind | Doc |
|------|------|-----|
| `Form` | type | Parsed url-encoded body, queryable by field name. |
| `FormBuilder` | type | Builder for url-encoded request bodies. |

## `std::http::health`

Liveness and readiness endpoints are ordinary handlers over `std::lifecycle`: answer 200 from a liveness route, and 200/503 from `lifecycle::is_ready()` on a readiness route, which drops to false on its own when shutdown begins. A probe registry with per-check timeouts belongs in an application package.

*No items exported yet.*

## `std::http::middleware`

Composable middleware: request_id, cors, security_headers, hsts, cache_control, etag, rate_limit, body_limit, timeout, compress_gzip, logger, recoverer, basic_auth, bearer_auth, safe_defaults.

| Item | Kind | Doc |
|------|------|-----|
| `CacheControl` | type | Cache-Control policy. `CacheControl::no_store()` never caches; `CacheControl::immutable_for(seconds)` marks a content-hashed asset immutable. |
| `Chain` | type | Helper for composing middleware in a single value. |
| `CorsConfig` | type | CORS configuration. `CorsConfig::permissive()` allows any origin and the common verbs; `CorsConfig::new(origin, methods, headers, max_age)` spells one out. |
| `HstsConfig` | type | HSTS configuration. `HstsConfig::safe_default()` is one year for this host; `HstsConfig::strict()` is two years with subdomains and preload. |
| `RateLimit` | type | Token-bucket budget. `RateLimit::per_ip(capacity, refill_per_sec)`. |
| `SecurityHeaders` | type | Security-header preset. `SecurityHeaders::strict()` adds CSP / COOP / Permissions-Policy on top of the baseline; `SecurityHeaders::off()` emits nothing. |
| `Handler` | trait | Anything serving (Request, Params) -> Response. |
| `accepts_gzip` | fn | Check an Accept-Encoding header for a gzip token. Available in interp + compiled. |
| `basic_auth` | fn | `basic_auth(inner, realm: String) -> Handler` - adds `WWW-Authenticate: Basic realm="..."` to a 401 response. Decode credentials with `middleware::decode_basic_auth`. |
| `bearer_auth` | fn | `bearer_auth(inner, realm: String) -> Handler` - adds `WWW-Authenticate: Bearer` to a 401 response. Verify tokens with `middleware::bearer_ok`. |
| `bearer_ok` | fn | Run a verify closure on the request's Bearer token; false (without calling verify) when no Bearer header is present. Available in interp + compiled. |
| `body_limit` | fn | `body_limit(inner, max_bytes: i64) -> Handler` - responses larger than the budget become 413. |
| `cache_control` | fn | `cache_control(inner, config: CacheControl) -> Handler` - sets `Cache-Control`. Example: `middleware::cache_control(app, middleware::CacheControl::no_store())`. |
| `compress_gzip` | fn | `compress_gzip(inner) -> Handler` - advertises negotiated compression with `Vary: Accept-Encoding`; pair with `middleware::accepts_gzip` to decide per request. |
| `cors` | fn | `cors(inner, config: CorsConfig) -> Handler` - CORS response headers. Example: `middleware::cors(app, middleware::CorsConfig::permissive())`. |
| `decode_basic_auth` | fn | Decode a Basic-auth Authorization header into (user, password). Interp tier. |
| `etag` | fn | `etag(inner) -> Handler` - sets a strong `ETag` derived from the response body, so the same body always yields the same validator. |
| `hsts` | fn | `hsts(inner, config: HstsConfig) -> Handler` - sets `Strict-Transport-Security`. Example: `middleware::hsts(app, middleware::HstsConfig::safe_default())`. |
| `logger` | fn | `logger(inner) -> Handler` - writes one `[http] <status> <bytes>b` line per response to stderr. |
| `new_request_id` | fn | Generate a process-monotonic request id string. Available in interp + compiled. |
| `rate_limit` | fn | `rate_limit(inner, config: RateLimit) -> Handler` - token-bucket limiter; past the budget the response becomes 429 with `Retry-After`. Example: `middleware::rate_limit(app, middleware::RateLimit::per_ip(100, 10))`. |
| `recoverer` | fn | `recoverer(inner) -> Handler` - replaces a 5xx response body with a fixed `internal server error`, so handler internals never leak. |
| `request_id` | fn | `request_id(inner) -> Handler` - stamps `X-Request-Id` on every response, using a process-monotonic `req-<n>` counter so a chain's output is identical on every tier. |
| `safe_defaults` | fn | `safe_defaults(inner) -> Handler` - strict security headers, HSTS, and a request id in one wrapper. |
| `security_headers` | fn | `security_headers(inner, preset: SecurityHeaders) -> Handler` - X-Content-Type-Options, X-Frame-Options, and Referrer-Policy; the `strict` preset adds CSP, COOP, and Permissions-Policy. Example: `middleware::security_headers(app, middleware::SecurityHeaders::strict())`. |
| `tag` | fn | Wrap a handler (`tag(inner) -> Handler`), prepending `mw:` to each response body. Deterministic composition primitive; available in interp + compiled. |
| `timeout` | fn | `timeout(inner, budget_ms: i64) -> Handler` - stamps the budget as `X-Timeout-Ms` for downstream proxies. |

## `std::http::multipart`

RFC 7578 multipart/form-data streaming parser.

| Item | Kind | Doc |
|------|------|-----|
| `Config` | type | Per-form size, part-count, and disk-spill limits. |
| `Form` | type | Parsed multipart envelope: fields + file parts. |
| `Part` | type | One field or file entry from a multipart body. |
| `PartData` | type | In-memory bytes or spilled-to-disk path for a part. |
| `parse` | fn | Stream-parse from any Read source into a Form. |

## `std::http::native_client`

Goroutine-driven HTTP/1.1 client over std::net (no ureq, no blocking pool).

| Item | Kind | Doc |
|------|------|-----|
| `Client` | type | Native h1 client (Rust-side; full builder surface). |
| `Error` | type | Connect / Tls / Http / Redirect / Timeout / Io. |
| `delete` | fn | One-shot DELETE → Result<Response, Error>. Interp tier. |
| `get` | fn | One-shot GET → Result<Response, Error>. Interp tier (compiled tier shares http::get). |
| `post` | fn | One-shot POST: `(url, body, content_type)`. Interp tier. |
| `put` | fn | One-shot PUT: `(url, body, content_type)`. Interp tier. |

## `std::http::proxy`

Reverse proxy on top of http::Client. Director-style request mutator + hop-by-hop strip + error handler.

| Item | Kind | Doc |
|------|------|-----|
| `Director` | type | Fn(&mut Request) request mutator (Rust-side). |
| `Proxy` | type | Reverse-proxy handler (Rust-side). |
| `forward` | fn | One-shot upstream forward: `(url, method, body) -> Result<Response, Error>`. Interp tier. |

## `std::http::query`

A request's query string is already parsed: read `request.query` for the raw text and `request.query_pairs` for the decoded name/value pairs.

*No items exported yet.*

## `std::http::router`

Go 1.22-class ServeMux: method-aware path patterns with parameter captures + prefix routes.

| Item | Kind | Doc |
|------|------|-----|
| `Params` | type | Captured path parameters. Read inside a handler with `r.path_value(name) -> String`; returns `""` for an undeclared name. All tiers. |
| `Router` | type | Routing table. Build with `Router::new()`, register routes via the verb methods, then pass to `http::serve`. Verb methods return the router so they chain with `|>`. |
| `Handler` | trait | Anything callable as `Fn(Request, Params) -> Response`. |
| `add` | fn | Register a pattern-only route: `(router, method, pattern)`. Used with `lookup` for low-level dispatch. |
| `lookup` | fn | Find the index of the first route matching `(method, path)`. Returns `Option<i64>`. |
| `new` | fn | Allocate a fresh Router handle. |

## `std::http::session`

Signs and verifies a session payload. The cookie itself - name, attributes, expiry, a server-side store, id rotation on privilege change, revocation - is application policy and belongs in a session package built on these two.

| Item | Kind | Doc |
|------|------|-----|
| `sign` | fn | Sign session data into a tamper-evident cookie value. |
| `verify` | fn | Verify and decode a signed session cookie value. |
| `with_session` | fn | Run a closure with the session bound; persist any mutations. |

## `std::http::sse`

Server-Sent Events (text/event-stream) emitter with heartbeat ticks and retry hint.

| Item | Kind | Doc |
|------|------|-----|
| `Event` | type | One SSE event (id, event, data, retry). |
| `Stream` | type | Active SSE stream - handler writes events through it (Rust-side). |
| `encode_comment` | fn | Render a `:`-prefixed keepalive line. Available in interp + compiled. |
| `encode_event` | fn | Render one event block as a string: `(event, data, id) -> String`. Available in interp + compiled. |
| `encode_retry` | fn | Render a `retry:` reconnect-hint directive in milliseconds. Available in interp + compiled. |

## `std::http::state`

Dependency injection is closure capture: build the router from closures that capture the pool, the cache, and the configuration, and each handler reads what it captured. A captured heap value is shared, so one map serves every request.

*No items exported yet.*

## `std::http::static_files`

Caching static-file handler: ETag, Last-Modified, byte ranges, MIME sniff.

| Item | Kind | Doc |
|------|------|-----|
| `FileServer` | type | Static-file handler rooted at a directory (Rust-side; streaming). |
| `mime_for_path` | fn | Guess a MIME type from a file path's extension. Available in interp + compiled. |
| `serve_file` | fn | Read a single file and return it as a Response struct. Interp tier. |

## `std::http::websocket`

RFC 6455 WebSocket support. Server-side accept + send_text / send_binary / ping / pong / close.

| Item | Kind | Doc |
|------|------|-----|
| `Error` | type | Io / Protocol / BadHandshake. |
| `Message` | type | Text / Binary / Ping / Pong / Close. |
| `WebSocket` | type | Accepted WebSocket connection (Rust-side framing). |
| `accept` | fn | Upgrade an incoming Request to a WebSocket (Rust-side). |
| `accept_key` | fn | Compute RFC 6455 Sec-WebSocket-Accept from a client nonce. Available in interp + compiled. |
| `close` | fn | close(ws) -> Result<(), Error>: send a close frame and release the handle. |
| `connect` | fn | connect(url) -> Result<i64, Error>: client TCP connect + RFC 6455 upgrade; returns a WebSocket handle. |
| `is_websocket_upgrade` | fn | Test whether an incoming Request carries a WebSocket upgrade handshake. Interp tier. |
| `recv` | fn | recv(ws) -> Result<String, Error>: next text message; Err on close/error. |
| `send_binary` | fn | send_binary(ws, data) -> Result<(), Error>: send one binary frame. |
| `send_text` | fn | send_text(ws, s) -> Result<(), Error>: send one text frame. |
| `serve` | fn | serve(addr, handler) -> Result<(), Error>: bind, upgrade each connection, dispatch the handler's handle(self, ws) per connection. |

## `std::http_h3`

HTTP/3 over QUIC. std::http_h3 is the retained 0.27 spelling; no std::http::h3 alias.

| Item | Kind | Doc |
|------|------|-----|
| `Client` | type | HTTP/3 client. `new` validates against the Mozilla root store; `insecure` skips verification (dev only). Methods: `get`, `post`, `put`, `delete`, `head`, `request`. |
| `H3Error` | type | Transport / protocol error variants surfaced from quinn + h3. |
| `Handler` | trait | Per-request handler. `fn serve(&self, request: Request) -> Response`. |
| `serve` | fn | Run an HTTP/3 server bound to `addr` with TLS certificate + key paths and the supplied handler. |

## `std::httptest`

Fixtures for testing HTTP code. A handler is a function from a request to a response, so `record` calls one in memory; a test that is about the wire builds an `http::Server`, binds port 0, and reads the address back.

| Item | Kind | Doc |
|------|------|-----|
| `record` | fn | `record(handler, method, path, body) -> Result<Response, Error>` - calls `handler` with a request built in memory and answers its response. No socket, no port, no accept loop. |
| `server` | fn | server(status, body) -> String: starts an isolated loopback static-response server and returns its http:// base URL. Use http::get or http::Client as the test client. The server is test-process scoped and stops when that process exits. For a programmable server, build an `http::Server` over a `Router` and bind port 0. |

## `std::image`

Opaque RGBA8 image handles with PNG and JPEG codecs.

| Item | Kind | Doc |
|------|------|-----|
| `decode_base64` | fn | Decodes a base64 PNG or JPEG; returns zero for malformed input. |
| `encode_jpeg_base64` | fn | Encodes an image as base64 JPEG at quality 1 through 100. |
| `encode_png_base64` | fn | Encodes an image as lossless base64 PNG. |
| `filled` | fn | Allocates an image handle filled with a packed 0xRRGGBBAA colour. |
| `height` | fn | Returns an image height in pixels. |
| `new` | fn | Allocates a transparent image handle. |
| `pixel` | fn | Returns packed 0xRRGGBBAA, or -1 outside the image. |
| `set_pixel` | fn | Sets a packed 0xRRGGBBAA pixel and reports whether it was in bounds. |
| `width` | fn | Returns an image width in pixels. |

## `std::io`

Stream-oriented I/O abstractions and process standard streams.

| Item | Kind | Doc |
|------|------|-----|
| `BufReader` | type | Buffered wrapper around any `Reader`. |
| `BufWriter` | type | Buffered wrapper around any `Writer`. |
| `Error` | type | Errors raised by I/O operations. |
| `Reader` | trait | Pull-style byte source. |
| `Writer` | trait | Push-style byte sink. |
| `Copy` | fn | Copies all bytes from src to dst; returns the byte count. |
| `ReadAll` | fn | Drains a reader to a String. Mirrors Go's io.ReadAll. |
| `buffer_writer` | fn | `buffer_writer() -> i64` - a Writer handle collecting everything written to it; read it back with `io::contents`. |
| `close_writer` | fn | `close_writer(writer: i64)` - signals end of stream on a pipe Writer; later writes are rejected. |
| `contents` | fn | `contents(writer: i64) -> String` - everything written to a buffer or pipe Writer, as UTF-8 text. |
| `copy_n` | fn | `copy_n(dst: i64, src: i64, n: i64) -> Result<i64, errors::Error>` - copies at most `n` bytes and returns the count actually transferred. Go's `io.CopyN`. |
| `drain` | fn | `drain(src: i64) -> String` - reads a Reader handle to end of stream as UTF-8 text. |
| `limit_reader` | fn | `limit_reader(src: i64, limit: i64) -> i64` - a Reader yielding at most `limit` bytes from `src`, Go's `io.LimitReader`. Example: `io::drain(io::limit_reader(src, 5))`. |
| `multi_reader` | fn | `multi_reader(sources: Vec<i64>) -> i64` - a Reader draining each source in turn, Go's `io.MultiReader`. Example: `io::multi_reader(#[a, b])`. |
| `pipe` | fn | `pipe() -> (i64, i64)` - a connected `(reader, writer)` pair sharing one in-memory buffer. Reads return the bytes written so far and never block; `io::close_writer` marks the writer done. Example: `let r, w = io::pipe()`. |
| `stderr` | fn | Returns a handle to the process's standard error stream. |
| `stdin` | fn | Returns a handle to the process's standard input stream. Use read_line(&mut String) for interactive prompts. |
| `stdout` | fn | Returns a handle to the process's standard output stream. |
| `string_reader` | fn | `string_reader(text: String) -> i64` - a Reader handle over an in-memory buffer. Reader and Writer handles are plain integers, so the adapters below compose by value. Example: `let src = io::string_reader("hello")`. |
| `tee_reader` | fn | `tee_reader(src: i64, sink: i64) -> i64` - a Reader mirroring every byte read from `src` into the Writer `sink`, Go's `io.TeeReader`. |
| `write` | fn | `write(writer: i64, text: String) -> i64` - appends text to a Writer handle and returns the byte count accepted. |

## `std::iter`

Sequence adapters: map, filter, fold, zip, enumerate, chain, etc. A `Vec` argument is traversed eagerly; an `Iterator` argument keeps the adapter lazy and answers with another iterator.

| Item | Kind | Doc |
|------|------|-----|
| `all` | fn | True if every element satisfies f. |
| `any` | fn | True if any element satisfies f. |
| `chain` | fn | Concatenates two sequences. |
| `chunk_by` | fn | Groups elements into a map keyed by f. |
| `chunks` | fn | Non-overlapping chunks of length n. |
| `collect` | fn | Materializes a sequence into a Vec. |
| `count` | fn | Number of elements. |
| `count_by` | fn | Counts elements per key derived by f. |
| `dedup` | fn | Removes consecutive duplicate elements. |
| `empty` | fn | Empty Vec. |
| `enumerate` | fn | Pairs each element with its index. |
| `filter` | fn | Returns elements where f is true. |
| `filter_map` | fn | Maps each element and keeps the Some results. |
| `find` | fn | First element satisfying f, or None. |
| `find_map` | fn | First Some result of f over the sequence. |
| `flat_map` | fn | Maps f and flattens one level. |
| `flatten` | fn | Flattens a Vec<Vec<T>> into Vec<T>. |
| `fold` | fn | Reduces a sequence with an accumulator. |
| `for_each` | fn | Applies f to each element for its side effect. |
| `map` | fn | Applies f to each element. Returns a Vec for a Vec argument, and another iterator for an iterator argument. |
| `max` | fn | Largest element, or None when empty. |
| `max_by` | fn | Largest element by the comparison closure. |
| `max_by_key` | fn | Element with the largest derived key. |
| `min` | fn | Smallest element, or None when empty. |
| `min_by` | fn | Smallest element by the comparison closure. |
| `min_by_key` | fn | Element with the smallest derived key. |
| `once` | fn | Single-element Vec containing value. |
| `pairwise` | fn | Consecutive overlapping pairs. |
| `partition` | fn | Splits into (matching, non-matching) by f. |
| `position` | fn | Index of the first element satisfying f, or None. |
| `product` | fn | Product of i64 or f64 elements. |
| `product_by` | fn | Product of f(element) over the sequence. |
| `range` | fn | Half-open integer sequence [start, end). |
| `range_inclusive` | fn | Closed integer sequence [start, end]. |
| `reduce` | fn | Folds with the first element as the initial accumulator. |
| `repeat` | fn | A value repeated n times. |
| `rev` | fn | Returns the elements in reverse order. |
| `scan` | fn | Folds while yielding each intermediate accumulator. |
| `skip` | fn | All elements after the first n. |
| `skip_while` | fn | Elements after the leading run satisfying f. |
| `sort_by` | fn | Sorted copy ordered by the comparison closure. |
| `sort_by_key` | fn | Sorted copy ordered by a derived key. |
| `step_by` | fn | Every step-th element, starting at index 0. |
| `sum` | fn | Sum of i64 or f64 elements. |
| `sum_by` | fn | Sum of f(element) over the sequence. |
| `take` | fn | First n elements. |
| `take_while` | fn | Leading run of elements satisfying f. |
| `unzip` | fn | Splits a sequence of pairs into two Vecs. |
| `windows` | fn | Overlapping windows of width n. |
| `zip` | fn | Pairs elements from two sequences. |

## `std::jwt`

RFC 7519 tokens. Signs with HS256 / HS384 / HS512, ES256, and EdDSA; verifies those plus the RS256 / RS384 / RS512 family every mainstream identity provider mints with. Claims cross the boundary as JSON text.

| Item | Kind | Doc |
|------|------|-----|
| `header` | fn | `header(token) -> Result<String, errors::Error>` - the JOSE header as JSON, read WITHOUT verifying the signature. Read `kid` to choose a key from a key set; nothing in it is trustworthy until `verify` succeeds. |
| `sign_eddsa` | fn | Sign with Ed25519 from a PEM-encoded private key. |
| `sign_es256` | fn | Sign with ECDSA P-256 from a PEM-encoded private key. |
| `sign_hs` | fn | Sign claims with HMAC-SHA family using a shared key. |
| `verify` | fn | `verify(token, alg, key, leeway_secs, issuer, audience) -> Result<String, errors::Error>` - the verifier a service protecting an endpoint uses. One entry point for every algorithm, RS* included; `key` is the shared secret for HS* and the PEM public key for the rest. `issuer` and `audience` are enforced when non-empty - leaving them empty accepts a token minted for another service by anyone sharing the key. |
| `verify_eddsa` | fn | Verify an EdDSA token against a PEM-encoded public key. Prefer `verify`, which also enforces the issuer and the audience. |
| `verify_es256` | fn | Verify an ES256 token against a PEM-encoded public key. Prefer `verify`, which also enforces the issuer and the audience. |
| `verify_hs` | fn | Verify an HS* token against a shared key with a clock-skew allowance. Prefer `verify`, which also enforces the issuer and the audience. |

## `std::lifecycle`

Process readiness and graceful shutdown, with systemd sd_notify. Shutdown is observed, not dispatched: wait for it, then drain with ordinary statements - `spawn(|| serve())`, `lifecycle::ready()`, `lifecycle::await_shutdown()`, then the cleanup.

| Item | Kind | Doc |
|------|------|-----|
| `await_shutdown` | fn | `await_shutdown()` - block until shutdown begins, whether from SIGTERM, SIGINT, or `shutdown()`. The statements after it are the drain sequence. |
| `is_ready` | fn | `is_ready() -> bool` - whether the process is ready to serve. False before `ready()` and once shutdown has begun, so a readiness probe fails ahead of the drain. |
| `is_shutting_down` | fn | `is_shutting_down() -> bool` - whether shutdown has begun. A long-running worker polls it to leave on its own terms. |
| `notify_status` | fn | `notify_status(message: String)` - report free-text status to the service manager (`sd_notify(STATUS=...)`). |
| `ready` | fn | `ready()` - declare the process ready to serve traffic; emits `sd_notify(READY=1)` under a service manager. |
| `set_ready` | fn | `set_ready(ready: bool)` - set readiness explicitly. Readiness also drops on its own when shutdown begins. |
| `shutdown` | fn | `shutdown()` - begin the graceful shutdown sequence: readiness drops and every server stops accepting while in-flight requests finish. |

## `std::math`

Mathematical constants and f64 functions (Go's math package shape).

| Item | Kind | Doc |
|------|------|-----|
| `abs` | fn | Absolute value of x. |
| `acos` | fn | Arccosine (radians). |
| `asin` | fn | Arcsine (radians). |
| `atan` | fn | Arctangent (radians). |
| `atan2` | fn | Four-quadrant arctangent of y/x. |
| `cbrt` | fn | Cube root. |
| `ceil` | fn | Smallest integer ≥ x. |
| `clamp` | fn | Constrain x to the inclusive range [lo, hi]. |
| `copysign` | fn | Magnitude of x with sign of y. |
| `cos` | fn | Cosine (radians). |
| `cosh` | fn | Hyperbolic cosine. |
| `exp` | fn | e^x. |
| `exp2` | fn | 2^x. |
| `floor` | fn | Largest integer ≤ x. |
| `hypot` | fn | Euclidean distance √(x²+y²). |
| `is_inf` | fn | Reports whether x is infinite. |
| `is_nan` | fn | Reports whether x is NaN. |
| `ln` | fn | Natural logarithm. |
| `log` | fn | Logarithm with given base. |
| `log10` | fn | Base-10 logarithm. |
| `log2` | fn | Base-2 logarithm. |
| `max` | fn | Greater of two values. |
| `min` | fn | Lesser of two values. |
| `positive_diff` | fn | max(x-y, 0). |
| `pow` | fn | x raised to the power y. |
| `rem` | fn | Floating-point remainder x%y. |
| `round` | fn | Nearest integer, half away from zero. |
| `sin` | fn | Sine (radians). |
| `sinh` | fn | Hyperbolic sine. |
| `sqrt` | fn | Square root. |
| `tan` | fn | Tangent (radians). |
| `tanh` | fn | Hyperbolic tangent. |
| `trunc` | fn | Integer part of x. |
| `E` | const | Euler's number e. |
| `INF` | const | Positive infinity. |
| `LN_10` | const | Natural log of 10. |
| `LN_2` | const | Natural log of 2. |
| `LOG10_E` | const | Base-10 logarithm of e. |
| `LOG2_E` | const | Base-2 logarithm of e. |
| `MAX_F64` | const | Largest finite f64 value. |
| `MIN_POSITIVE_F64` | const | Smallest positive normal f64 value. |
| `NAN` | const | Not-a-number value. |
| `NEG_INF` | const | Negative infinity. |
| `PHI` | const | Golden ratio φ. |
| `PI` | const | Archimedes' constant π. |
| `SQRT_2` | const | √2. |

## `std::math::big`

Arbitrary-precision integers (num-bigint).

| Item | Kind | Doc |
|------|------|-----|
| `Int` | type | Arbitrary-precision signed integer. |
| `Uint` | type | Arbitrary-precision unsigned integer. |
| `factorial` | fn | Computes n! as an Int. |
| `int_abs` | fn | Absolute value of an Int. |
| `int_add` | fn | Sum of two Ints. |
| `int_cmp` | fn | Three-way comparison of two Ints (-1, 0, 1). |
| `int_div` | fn | Truncated quotient of two Ints. |
| `int_from_i64` | fn | Converts an i64 into an Int. |
| `int_from_str` | fn | Parses a decimal string into an Int. |
| `int_gcd` | fn | Greatest common divisor of two Ints. |
| `int_is_negative` | fn | Reports whether the Int is less than zero. |
| `int_is_positive` | fn | Reports whether the Int is greater than zero. |
| `int_is_zero` | fn | Reports whether the Int is zero. |
| `int_lcm` | fn | Least common multiple of two Ints. |
| `int_mul` | fn | Product of two Ints. |
| `int_neg` | fn | Negation of an Int. |
| `int_pow` | fn | Int raised to a non-negative power. |
| `int_rem` | fn | Remainder of two Ints. |
| `int_sub` | fn | Difference of two Ints. |
| `int_to_hex` | fn | Hexadecimal string form of an Int. |
| `int_to_i64` | fn | Narrows an Int to i64 where it fits. |
| `int_to_str` | fn | Decimal string form of an Int. |
| `uint_add` | fn | Sum of two Uints. |
| `uint_bit_len` | fn | Number of significant bits in a Uint. |
| `uint_from_str` | fn | Parses a decimal string into a Uint. |
| `uint_from_u64` | fn | Converts a u64 into a Uint. |
| `uint_is_zero` | fn | Reports whether the Uint is zero. |
| `uint_mul` | fn | Product of two Uints. |
| `uint_pow` | fn | Uint raised to a non-negative power. |
| `uint_pow_mod` | fn | Modular exponentiation of a Uint. |
| `uint_to_hex` | fn | Hexadecimal string form of a Uint. |
| `uint_to_str` | fn | Decimal string form of a Uint. |
| `uint_to_u64` | fn | Narrows a Uint to u64 where it fits. |

## `std::math::bits`

Integer bit-manipulation operations (Go's math/bits shape).

| Item | Kind | Doc |
|------|------|-----|
| `add` | fn | x + y + carry; returns (sum, carry_out). |
| `count_ones` | fn | Number of set bits (popcount). |
| `count_zeros` | fn | Number of clear bits. |
| `div` | fn | 128-bit dividend / 64-bit divisor; returns (quotient, remainder). |
| `leading_zeros` | fn | Leading zero bit count. |
| `len` | fn | Minimum bits required to represent x. |
| `mul` | fn | Full 128-bit product; returns (hi, lo). |
| `reverse_bits` | fn | Reverses bit order of x. |
| `reverse_bytes` | fn | Reverses byte order of x. |
| `rotate_left` | fn | Rotates x left by n bits. |
| `rotate_right` | fn | Rotates x right by n bits. |
| `sub` | fn | x - y - borrow; returns (diff, borrow_out). |
| `trailing_zeros` | fn | Trailing zero bit count. |

## `std::math::rand`

Deterministic pseudo-random number generation.

| Item | Kind | Doc |
|------|------|-----|
| `Rng` | type | SplitMix64-based RNG. |

## `std::metrics`

Prometheus-compatible primitives (Counter, Gauge, Histogram) and a Registry rendering the standard text-exposition format.

| Item | Kind | Doc |
|------|------|-----|
| `Counter` | type | Monotonic-increasing u64 counter (lock-free). |
| `Gauge` | type | Set / inc / dec gauge (lock-free). |
| `Histogram` | type | Bucketed observation histogram with sum and count. |
| `Metric` | type | Enum holding any of the three primitives for registry storage. |
| `Registry` | type | Ordered collection of metrics; `render()` emits the Prometheus text-exposition format. |
| `serve_metrics` | fn | Mounts a registry on `/metrics` over the existing http server loop. |

## `std::mime`

RFC 2045 media type parsing, parameter extraction, and extension lookup.

| Item | Kind | Doc |
|------|------|-----|
| `boundary` | fn | Return the multipart `boundary` parameter, or empty. |
| `charset` | fn | Return the `charset` parameter, or empty. |
| `extension_by_type` | fn | Canonical extension (no leading dot) for a media type, or empty. |
| `is_valid` | fn | Return true iff the string parses as a valid media type. |
| `param` | fn | Return an arbitrary parameter by key, or empty. |
| `parse` | fn | Canonical `type/subtype` form of the input, or empty on parse failure. |
| `sub` | fn | Subtype (e.g. `html`) of a media type, or empty. |
| `top` | fn | Top-level type (e.g. `text`) of a media type, or empty. |
| `type_by_extension` | fn | Canonical media type for a filename extension (dot optional), or empty. |

## `std::net`

TCP/UDP networking primitives.

| Item | Kind | Doc |
|------|------|-----|
| `TcpListener` | type | Accepts incoming TCP connections. |
| `TcpStream` | type | Bidirectional TCP byte stream; supports read/write, TLS upgrade, close, and read/write timeout setters. |
| `UdpSocket` | type | Bound UDP socket for datagram I/O. |
| `UnixListener` | type | Unix-domain socket listener. |
| `UnixStream` | type | Connected Unix-domain byte stream. |
| `lookup` | fn | Resolves a hostname to its IP addresses. |

## `std::net::ip`

String-level IPv4 / IPv6 parsing and classification helpers.

| Item | Kind | Doc |
|------|------|-----|
| `is_loopback` | fn | Reports whether the IP is a loopback address. |
| `is_multicast` | fn | Reports whether the IP is a multicast address. |
| `is_private` | fn | Reports whether the IP is in a private range. |
| `is_unspecified` | fn | Reports whether the IP is the unspecified address. |
| `is_v4` | fn | Reports whether the string is a valid v4 IP. |
| `is_v6` | fn | Reports whether the string is a valid v6 IP. |
| `is_valid` | fn | Reports whether the string is a valid v4 or v6 IP. |
| `octets` | fn | Byte octets of the IP as a Vec. |
| `parse` | fn | Parses an IP string, returning its canonical form or None. |
| `to_string` | fn | Canonical lowercase string form of the IP. |

## `std::net::netip`

Typed IP-address parsing, classification, and addr:port helpers (Go's net/netip shape).

| Item | Kind | Doc |
|------|------|-----|
| `host_of` | fn | Host portion of an addr:port string, or empty on parse failure. |
| `is_loopback` | fn | Return true iff the string parses as a loopback IP (127.0.0.1 / ::1). |
| `is_multicast` | fn | Return true iff the string parses as a multicast IP. |
| `is_private` | fn | Return true iff the IP is RFC1918 (v4) or ULA fc00::/7 (v6). |
| `is_unspecified` | fn | Return true iff the string parses as the unspecified IP (0.0.0.0 / ::). |
| `is_v4` | fn | Return true iff the string parses as a v4 IP. |
| `is_v6` | fn | Return true iff the string parses as a v6 IP. |
| `is_valid` | fn | Return true iff the string parses as a v4 or v6 IP. |
| `join_addr_port` | fn | Compose an addr:port string from host and port, or empty on failure. |
| `normalize` | fn | Canonical lowercase form of the IP, or empty string on parse failure. |
| `port_of` | fn | Port portion of an addr:port string, or -1 on parse failure. |

## `std::net::smtp`

Sends one message per call, so an application can mail a password reset, an address verification, a magic link, or a security notice. A pool, a queue, retries, and bounce handling are application policy and belong in a package built on these. Port 465 speaks TLS from the first byte; any other port starts in the clear and upgrades through STARTTLS when the server offers it, and credentials are refused rather than sent to a server offering no encryption.

| Item | Kind | Doc |
|------|------|-----|
| `send` | fn | `send(addr, from, to, subject, body) -> Result<(), errors::Error>` - one unauthenticated message. `to` may name several recipients, comma-separated. A CR or LF in an address or the subject is replaced with a space, so a caller-supplied value cannot end the header and add its own. |
| `send_auth` | fn | `send_auth(addr, from, to, subject, body, username, password) -> Result<(), errors::Error>` - the shape a transactional mail provider takes. Authenticates with AUTH PLAIN or AUTH LOGIN, whichever the server advertises, and only over TLS. |

## `std::net::url`

Network URL parsing and component escaping; never use filesystem-path rules.

| Item | Kind | Doc |
|------|------|-----|
| `Url` | type | Parsed URL. |
| `path_escape` | fn | Percent-encodes a URL path segment. |
| `path_unescape` | fn | Inverse of `path_escape`. |
| `query_escape` | fn | Percent-encodes a query parameter. |
| `query_unescape` | fn | Inverse of `query_escape`. |

## `std::option`

Data-last Option combinators for pipeline chaining: map, filter, unwrap_or, and_then, etc.

| Item | Kind | Doc |
|------|------|-----|
| `and_then` | fn | Chains a fallible step: Some(v) -> f(v), None stays None. |
| `expect` | fn | Returns the payload, panicking with the message when the value is None. |
| `filter` | fn | Keeps Some(v) only when the predicate holds. |
| `flatten` | fn | Collapses Option<Option<T>> one level. |
| `is_none` | fn | True for None. |
| `is_some` | fn | True for Some. |
| `iter` | fn | Zero-or-one element sequence view. |
| `map` | fn | Transforms the Some payload, None stays None. |
| `ok_or` | fn | Converts Some to Ok, or None to Err with the provided error. |
| `ok_or_else` | fn | Converts Some to Ok, or None to Err from a fallback closure. |
| `or` | fn | First Some of self and the alternative. |
| `or_else` | fn | First Some of self and a lazily built alternative. |
| `unwrap` | fn | Returns the payload, panicking when the value is None. |
| `unwrap_or` | fn | Unwraps with a fallback value for None. |
| `unwrap_or_else` | fn | Unwraps with a lazily computed fallback for None. |
| `zip` | fn | Pairs two Somes into Some((a, b)). |

## `std::os`

Operating-system identity.

| Item | Kind | Doc |
|------|------|-----|
| `arch` | fn | Returns the target CPU architecture (e.g. "x86_64"). |
| `family` | fn | Returns "unix" or "windows" for the running OS family. |

## `std::os::exec`

Deprecated compatibility facade for child processes; new code uses std::process.

| Item | Kind | Doc |
|------|------|-----|
| `Child` | type | Handle to a still-running child supporting wait / kill. |
| `Path` | type | Immutable UTF-8 lexical path value with value-returning operations. |
| `Pipeline` | type | Multi-stage subprocess pipeline (stdout-to-stdin chain). |
| `Signal` | type | Portable signal selector (Term/Kill/Stop/Cont/Hup/Int/Usr1/Usr2/Pipe/Quit). |
| `kill` | fn | Best-effort SIGTERM by pid; returns true on success. |
| `kill_group` | fn | Send SIGTERM to the entire process group (Unix); best-effort TerminateProcess on Windows. |
| `pipeline_run` | fn | Run a Vec<String> of shell-tokenised commands as a stdout-to-stdin pipeline. |
| `run` | fn | One-shot: runs a program with args, captures stdout/stderr, returns Result<{stdout, stderr, code}, String>. |
| `signal` | fn | Send an arbitrary signal number to a pid; returns true on success. |
| `spawn` | fn | Non-blocking launch; returns the child PID as Result<i64, errors::Error>. |
| `spawn_piped` | fn | Spawns a child with piped stdin/stdout; returns Result<Child, errors::Error>. The Child's write_stdin / close_stdin / read_line / read_stdout / wait / kill methods drive it interactively. |
| `wait_timeout` | fn | Wait up to N ms for a pid to exit; returns exit code, -1 on timeout, -2 on error. |

## `std::os::signal`

POSIX-style signal subscription (Go's os/signal shape).

| Item | Kind | Doc |
|------|------|-----|
| `Notifier` | type | Returned by `on(sig)`; supports wait / try_wait. |
| `Signal` | type | Opaque signal name; constructors live in `sigs`. |
| `on` | fn | Subscribes to a signal; returns a Notifier. |
| `try_wait` | fn | Non-blocking poll: returns true if the subscribed signal has fired. |
| `wait` | fn | Blocks the calling goroutine until the subscribed signal fires. |

## `std::os::user`

POSIX user / group lookup. Unix-backed by `nix`; Windows falls back to env vars.

| Item | Kind | Doc |
|------|------|-----|
| `current_gid` | fn | gid of the current process user, or -1 on non-unix. |
| `current_home` | fn | Home directory of the current process user. |
| `current_name` | fn | Login name of the current process user, or empty string. |
| `current_uid` | fn | uid of the current process user, or -1 on non-unix. |
| `lookup_name` | fn | uid for the user with the given login name, or -1 if not found. |
| `lookup_uid` | fn | Login name for the given uid, or empty string if unknown. |

## `std::panic`

Panic / `catch_unwind` integration.

| Item | Kind | Doc |
|------|------|-----|
| `panic` | builtin | Aborts the current goroutine with a message. |

## `std::path`

Lexical filesystem-path operations; platform path grammar, no URL parsing.

| Item | Kind | Doc |
|------|------|-----|
| `components` | fn | Returns Rust-like lexical path components. |
| `extension` | fn | Dotted extension as an Option. |
| `file_name` | fn | Final path component, or None. |
| `file_stem` | fn | File name without its extension. |
| `glob` | fn | `glob(pattern) -> Result<Vec<String>, errors::Error>` - filesystem paths matching a shell glob, sorted so every tier reports the same order. Supports `*`, `?`, `[abc]`, and `**` (this directory and every descendant). Relative patterns resolve against the working directory. Example: `let found = path::glob("src/**/*.gos")?`. |
| `is_absolute` | fn | Reports whether the path is absolute. |
| `join` | fn | Joins two path fragments. |
| `matches` | fn | `matches(pattern, name) -> bool` - Go `filepath.Match` shell-glob test over a single path segment: `*` and `?` never cross a `/`, `[abc]` is a character class. Spelled `matches` because `match` is a keyword. Example: `path::matches("*.gos", "main.gos")` is true, `path::matches("a*c", "a/c")` is false. |
| `normalize` | fn | Lexically normalizes the path. |
| `parent` | fn | Parent directory, or None at the root. |
| `prefixes` | fn | Returns cumulative Rust-like lexical path prefixes. |
| `split` | fn | Returns (dir, file) for the supplied path. |
| `starts_with` | fn | Reports whether the path begins with a prefix component-wise. |
| `unique_prefixes` | fn | Returns sorted unique prefixes for newline-delimited paths. |
| `walk` | fn | Recursively visits every descendant entry under a directory, the path-module spelling of fs::walk_dir. |

## `std::pprof`

Runtime profiles in the text format `go tool pprof` reads, plus a Chrome-trace scheduler capture.

| Item | Kind | Doc |
|------|------|-----|
| `block_profile` | fn | Text profile of microseconds parked on channels, I/O, and timers since process start. |
| `cpu_profile` | fn | Sampled CPU profile over the given milliseconds; a timer reads the running stack. |
| `execution_trace` | fn | Chrome trace JSON of scheduler spawn/park/unpark events; blocks for the given milliseconds. |
| `goroutine_profile` | fn | Text profile with one sample per live goroutine and its last-known frame. |
| `heap_profile` | fn | Sampled allocation profile over the given milliseconds, weighted by bytes. |
| `mutex_profile` | fn | Text profile of microseconds parked on synchronization since process start. |
| `route` | fn | Serves a `/debug/pprof/...` path, returning the body to write, or `None` for an unknown path. |

## `std::process`

Canonical process control and child-process API; std::os::exec is compatibility-only.

| Item | Kind | Doc |
|------|------|-----|
| `Child` | type | Handle to a still-running child supporting wait / kill. |
| `abort` | fn | Aborts the current process without unwinding. |
| `exit` | fn | Exits the current process with the given status code. |
| `id` | fn | Returns the current process ID. |
| `kill` | fn | Sends SIGKILL (or equivalent) to a Child. |
| `kill_group` | fn | Sends a signal to a process group (POSIX). |
| `pipeline_run` | fn | Runs a shell-tokenised pipeline and returns captured stdout/stderr plus the final exit code. |
| `run` | fn | One-shot: runs a program with args, captures stdout/stderr plus the exit code. |
| `run_in` | fn | run_in(program, args, dir, env): the same one-shot run with the child's working directory and environment supplied. An empty dir inherits the caller's; the env pairs override the inherited environment rather than replacing it, so a caller sets the two variables it cares about without restating PATH. |
| `signal` | fn | Sends a signal to a process by PID (POSIX). |
| `spawn` | fn | Spawns a child process and returns its PID. |
| `spawn_piped` | fn | Spawns a child with piped stdin/stdout; returns Result<Child, errors::Error>. The Child's write_stdin / close_stdin / read_line / read_stdout / wait / kill methods drive it interactively. |
| `wait_timeout` | fn | Waits for a child with a timeout (POSIX). |

## `std::regex`

Compiled regular expressions (Rust `regex` crate syntax; no backreferences or look-around).

| Item | Kind | Doc |
|------|------|-----|
| `Pattern` | type | Compiled pattern handle returned by `compile`. |
| `captures` | fn | Returns capture groups for the first match; index 0 is the full match. |
| `captures_all` | fn | Returns capture groups for every match in the text. |
| `compile` | fn | Parses a pattern into a reusable `Pattern` or returns an `Err`. |
| `count` | fn | Counts the non-overlapping matches without building them. |
| `find` | fn | Returns the first match as `(start, end, text)`, or `None`. |
| `find_all` | fn | Returns every non-overlapping match as `(start, end, text)`. |
| `is_match` | fn | Returns whether the pattern matches anywhere in the text. |
| `replace` | fn | Replaces the first match with the given replacement (supports `$N`). |
| `replace_all` | fn | Replaces every non-overlapping match. |
| `split` | fn | Splits the text on every pattern match. |

## `std::result`

Data-last Result combinators for pipeline chaining: map, map_err, unwrap_or_else, etc.

| Item | Kind | Doc |
|------|------|-----|
| `and_then` | fn | Chains a fallible step on the Ok payload. |
| `err` | fn | Err payload as an Option. |
| `is_err` | fn | True for Err. |
| `is_ok` | fn | True for Ok. |
| `map` | fn | Transforms the Ok payload, Err passes through. |
| `map_err` | fn | Transforms the Err payload, Ok passes through. |
| `ok` | fn | Ok payload as an Option. |
| `or_else` | fn | Recovers from Err with a fallback computation. |
| `unwrap_or` | fn | Unwraps Ok with a fallback value for Err. |
| `unwrap_or_else` | fn | Consumes the result, handling Err with a callback. |

## `std::runtime`

Goroutine / scheduler introspection and tuning.

| Item | Kind | Doc |
|------|------|-----|
| `arena_pop` | fn | Closes the innermost arena region, freeing its slabs. |
| `arena_push` | fn | Opens an arena region for bump allocation. |
| `cohort_cancel` | fn | Cancels the running goroutine's cohort, winding its siblings down without failing it. |
| `cohort_cancelled` | fn | Whether the running goroutine's cohort has been cancelled. A CPU-bound child polls this to cooperate at a point of its own choosing. |
| `cohort_join` | fn | Waits for every child of the running goroutine's cohort and answers `Result<(), errors::Error>`. |
| `cohort_pop` | fn | Closes the running goroutine's cohort, cancelling and joining anything still running. |
| `cohort_push` | fn | Opens a cohort on the running goroutine. Written `cohort { }` in source; the block desugars to this plus `cohort_join` and a deferred `cohort_pop`. |
| `cohorts` | fn | One descriptor line per live cohort, oldest id first: id, parent, completion policy, error disposition, outstanding count, and the spawn indices still running. A cohort is enumerable so a program can say what it is waiting on without joining it. |
| `collect_cycles` | fn | Requests collection of unreachable reference cycles; returns `()`. |
| `cycle_collection_supported` | fn | Reports whether this execution tier reclaims unreachable reference cycles. |
| `root` | fn | The root cohort's descriptor line, in the shape `cohorts()` answers, or an empty String when no cohort is open. The root is the one cohort every program has - `main` runs inside it - and the one whose drain bounds process exit. |
| `scheduler_stats_json` | fn | Returns a compact JSON snapshot of goroutine scheduler counters. |
| `set_panic_hook` | fn | Installs a hook invoked with the message on panic. |

## `std::slog`

Structured, levelled logging.

| Item | Kind | Doc |
|------|------|-----|
| `Field` | type | Key/value pair threaded through a logger. |
| `JsonHandler` | type | JSON-lines handler. |
| `Logger` | type | Logger handle. |
| `TextHandler` | type | Line-oriented handler. |
| `debug` | fn | Logs a JSON record at DEBUG level. |
| `error` | fn | Logs a JSON record at ERROR level. |
| `info` | fn | Logs a JSON record at INFO level. Trailing args are key/value pairs. |
| `warn` | fn | Logs a JSON record at WARN level. |

## `std::sort`

Explicit stable ordering and sorted-sequence search, the deliberate counterpart to Vec's unstable inherent `sort`.

| Item | Kind | Doc |
|------|------|-----|
| `binary_search` | fn | `binary_search(xs: Vec<T>, target: T) -> Option<i64>` - index of a matching element in an already-ascending sequence, `None` when absent. Example: `sort::binary_search(#[1, 3, 5], 3)` is `Some(1)`. |
| `partition_point` | fn | `partition_point(xs: Vec<T>, pivot: T) -> i64` - the count of elements strictly less than `pivot` in an already-ascending sequence, which is also the insertion index that keeps it sorted. Example: `sort::partition_point(#[1, 2, 6, 8], 5)` is `2`. |
| `sort_stable` | fn | `sort_stable(xs: Vec<T>) -> Vec<T>` - a fresh ascending sequence in which equal elements keep their input order. `xs.sort()` sorts in place and is unstable; this is the spelling that guarantees stability. Example: `let ordered = sort::sort_stable(#[3, 1, 2])`. |

## `std::strconv`

Conversions between strings and primitive numeric types.

| Item | Kind | Doc |
|------|------|-----|
| `format_f64` | fn | Renders an `f64` as a decimal string. |
| `format_i64` | fn | Renders an `i64` as a decimal string. |
| `format_i64_radix` | fn | Formats an i64 in the given base (2..=36). |
| `parse_bool` | fn | Parses `"true"` / `"false"` into a bool. |
| `parse_f64` | fn | Parses a decimal `f64`. |
| `parse_i64` | fn | Parses a decimal `i64`. |
| `parse_i64_radix` | fn | Parses an i64 from a string in the given base (2..=36). |
| `parse_u64` | fn | Parses a decimal `u64`. |
| `quote` | fn | Wraps a string in double quotes with escapes. |
| `unquote` | fn | Removes surrounding quotes and resolves escapes. |

## `std::strings`

String operations.

| Item | Kind | Doc |
|------|------|-----|
| `byte_at` | fn | Returns the UTF-8 byte at an index. |
| `byte_len` | fn | Returns the UTF-8 byte length. |
| `bytes` | fn | Returns the UTF-8 bytes of the string. |
| `center` | fn | Symmetric pad to `width` using the given pad character. |
| `chars` | fn | Returns a cursor over the string's Unicode scalar values; `collect` materialises it. |
| `contains` | fn | Returns whether the string contains a substring. |
| `contains_any` | fn | Reports whether the string contains any rune in a set. |
| `count` | fn | Counts non-overlapping occurrences of `needle`. |
| `ends_with` | fn | Returns whether the string ends with the given suffix. |
| `equal_fold` | fn | Case-insensitive Unicode string equality. |
| `find` | fn | Returns the byte position of the first match. |
| `find_any` | fn | Byte index of the first rune in a set, or None. |
| `join` | fn | Joins string parts with a separator. |
| `lines` | fn | Splits into lines, dropping line terminators. |
| `pad_left` | fn | Left-pads to `width` with the given character. |
| `pad_right` | fn | Right-pads to `width` with the given character. |
| `repeat` | fn | Concatenates n copies of the string. |
| `replace` | fn | Replaces every occurrence of `from` with `to`. |
| `replacen` | fn | Replaces the first n occurrences of a substring. |
| `rfind` | fn | Byte index of the last occurrence of a needle, or -1. |
| `rfind_any` | fn | Byte index of the last rune in a set, or None. |
| `rsplit_once` | fn | Splits on the last occurrence of `sep`; returns Option<(String, String)>. |
| `slice` | fn | Safe byte-range slice returning Result<String, errors::Error>. |
| `split` | fn | Splits a string by a delimiter. |
| `split_once` | fn | Splits on the first occurrence of `sep`; returns Option<(String, String)>. |
| `split_whitespace` | fn | Splits on runs of whitespace, dropping empty fields. |
| `splitn` | fn | Splits a string into at most `n` parts. |
| `starts_with` | fn | Returns whether the string starts with the given prefix. |
| `strip_prefix` | fn | Removes a leading prefix if present. |
| `strip_suffix` | fn | Removes a trailing suffix if present. |
| `substring` | fn | Byte-offset substring returning a String. |
| `to_bool` | fn | Parses exactly `true` / `false` to Option<bool>. |
| `to_f64` | fn | Strict full-string parse to Option<f64>. |
| `to_i64` | fn | Strict full-string parse to Option<i64>. |
| `to_lowercase` | fn | Lowercases every character. |
| `to_title` | fn | Title-cases the first letter of each word. |
| `to_uppercase` | fn | Uppercases every character. |
| `trim` | fn | Removes leading and trailing whitespace. |
| `trim_end` | fn | Removes trailing whitespace. |
| `trim_end_matches` | fn | Removes trailing characters in the given set. |
| `trim_matches` | fn | Removes characters in the given set from both ends. |
| `trim_start` | fn | Removes leading whitespace. |
| `trim_start_matches` | fn | Removes leading characters in the given set. |

## `std::sync`

Synchronisation primitives beyond channels.

| Item | Kind | Doc |
|------|------|-----|
| `AtomicBool` | type | Atomic boolean. |
| `AtomicI32` | type | Atomic 32-bit signed integer. |
| `AtomicI64` | type | Atomic 64-bit signed integer. |
| `AtomicU64` | type | Atomic 64-bit unsigned integer. |
| `Barrier` | type | Synchronisation barrier across goroutines. |
| `Channel` | type | Bidirectional channel handle. |
| `Map` | type | Concurrent key/value map. |
| `Mutex` | type | Mutual-exclusion lock. |
| `Once` | type | One-shot initialisation latch. |
| `RwLock` | type | Reader-writer lock. |
| `Shared` | type | A value several goroutines reach, every read and write taken under one lock. Build with `Shared::new(value)`; read with `get` or `with`, write with `set`, and read-modify-write with `update`, which holds the lock across the callback. |
| `WaitGroup` | type | Counts goroutines and waits for them to finish. |
| `channel` | fn | Creates a typed channel, returning (Sender, Receiver). |
| `channel_unbounded` | fn | Creates an explicit unbounded typed channel, returning (Sender, Receiver). |
| `shield` | fn | Runs a callable in a cohort exempt from cancellation and answers its value: a cancel landing on an enclosing cohort does not reach the work inside. The shielded region is still counted and still drained. |
| `with_timeout` | fn | Runs a callable on a child of a cohort bounded by a millisecond deadline, answering `Ok(value)` when it finished inside the bound and `Err` naming the bound when it did not. Cancellation is cooperative: a child that reaches no cancellation point runs on and is named in the cohort's drain report. |

## `std::testing`

Assertions and sub-test harness helpers.

| Item | Kind | Doc |
|------|------|-----|
| `Runner` | type | Sub-test collector. |
| `check` | fn | Asserts a condition. |
| `check_eq` | fn | Asserts equality, rendering a diff on failure. |
| `check_ok` | fn | Asserts a Result is Ok, recording without panicking. |
| `wait_for_scheduler_idle` | fn | Waits for the scheduler to become idle within a timeout. |

## `std::thread`

OS-thread scheduling hints and CPU introspection; user concurrency uses goroutines, not thread spawning.

| Item | Kind | Doc |
|------|------|-----|
| `num_cpus` | fn | Returns the number of logical CPUs available. |
| `yield_now` | fn | Hints to the scheduler to switch to another runnable thread. |

## `std::time`

Wall-clock and monotonic time facilities.

| Item | Kind | Doc |
|------|------|-----|
| `CivilResolution` | type | Explicit unique, gap, or fold result of resolving civil fields. |
| `CivilTime` | type | Calendar fields interpreted only with an explicit location. |
| `Duration` | type | Difference between two `Instant`s. |
| `Instant` | type | Monotonic point-in-time. |
| `Location` | type | Immutable IANA or fixed-offset time-zone location. |
| `SystemTime` | type | Wall-clock point-in-time. |
| `add_date` | fn | Adds calendar units and rejects ambiguous or nonexistent results. |
| `advance` | fn | `advance(ms) -> i64` - move a frozen wall clock forward and answer the new reading. Freezes at the current reading first when it is not already frozen. |
| `after` | fn | A `Receiver` that yields once after `Duration`, so a deadline composes with `select` and `while let` the way any other channel does. |
| `format_in` | fn | Formats an instant in an explicit civil-time location. |
| `format_rfc3339` | fn | Formats a `SystemTime` in RFC 3339 (`YYYY-MM-DDTHH:MM:SSZ`). |
| `freeze` | fn | `freeze(ms)` - pin the wall clock at `ms` since the epoch, so a test over anything time-dependent - a token's expiry, a rate-limit window, a cache's TTL - reads a clock it controls. The monotonic clock and `sleep` are untouched: this pins what the program is told the time is, not how long anything takes. |
| `is_frozen` | fn | `is_frozen() -> bool` - whether the wall clock is pinned. |
| `monotonic_ms` | fn | Monotonic clock reading in milliseconds. |
| `monotonic_nanos` | fn | Monotonic clock reading in nanoseconds. |
| `now` | fn | Wall-clock milliseconds since the Unix epoch; `Instant::now()` reads the monotonic clock. |
| `now_ms` | fn | Wall-clock milliseconds since the Unix epoch. |
| `now_nanos` | fn | Wall-clock nanoseconds since the Unix epoch. |
| `parse_rfc3339` | fn | Parses an RFC 3339 timestamp into a `SystemTime`. |
| `since_ms` | fn | Milliseconds elapsed since an earlier monotonic reading. |
| `sleep` | fn | Suspends the current goroutine for `Duration`. |
| `sleep_ctx` | fn | Sleeps unless the context fires first; false when it cancelled the wait. |
| `unfreeze` | fn | `unfreeze()` - return to the real wall clock. |
| `unix_ms` | fn | Current Unix time in milliseconds. |

## `std::tls`

Rustls-backed TLS support exposed through `http::serve_tls` and `net::TcpStream` TLS upgrades. The configuration constructors are host-runtime internals, not Gossamer callables.

*No items exported yet.*

## `std::trace`

W3C trace-context-compatible distributed tracing. Identifier types, request-scoped SpanContext, process-level Tracer, and OTLP JSON export.

| Item | Kind | Doc |
|------|------|-----|
| `EndedSpan` | type | Finalised span record; `to_otlp_json()` serialises for OTLP/HTTP export. |
| `Span` | type | Active span builder. Attributes, events, status; `end()` finalises and records. |
| `SpanContext` | type | Request-scoped trace + span pair, propagated through `std::context`. |
| `SpanGuard` | type | RAII guard returned by `enter_span`; restores the prior active span on drop. |
| `SpanId` | type | 64-bit span identifier. |
| `SpanStatus` | type | Span outcome: Unset / Ok / Error(message). |
| `TraceId` | type | 128-bit trace identifier (W3C trace-context format). |
| `Tracer` | type | Process-level span sink. `start_span`, `ended_spans`, `set_global`. |

## `std::unicode`

Unicode general-category predicates, casing, normalization, and segmentation.

| Item | Kind | Doc |
|------|------|-----|
| `combining_class` | fn | Canonical combining class (0-254) for r. |
| `fold_case` | fn | Simple case-folded comparison form for a string. |
| `grapheme_count` | fn | Number of UAX #29 grapheme clusters in a string. |
| `graphemes` | fn | UAX #29 extended grapheme clusters of a string. |
| `is_assigned` | fn | True if r is an assigned code point (not Cn). |
| `is_control` | fn | True if r is category Cc. |
| `is_digit` | fn | True if r is a decimal digit (category Nd). |
| `is_graphic` | fn | True if r is graphic (printable and not whitespace). |
| `is_letter` | fn | True if r is in general-category group L. |
| `is_lower` | fn | True if r is category Ll. |
| `is_mark` | fn | True if r is in general-category group M. |
| `is_nfc` | fn | True if a string is already in NFC. |
| `is_nfd` | fn | True if a string is already in NFD. |
| `is_nfkc` | fn | True if a string is already in NFKC. |
| `is_nfkd` | fn | True if a string is already in NFKD. |
| `is_number` | fn | True if r is any numeric (Nd|Nl|No). |
| `is_print` | fn | True if r is printable (not Cc/Cf/Cs/Co/Cn). |
| `is_punct` | fn | True if r is in general-category group P. |
| `is_space` | fn | True if r is whitespace (Z* plus HT/LF/VT/FF/CR/NEL). |
| `is_symbol` | fn | True if r is in general-category group S. |
| `is_title` | fn | True if r is category Lt. |
| `is_upper` | fn | True if r is category Lu. |
| `nfc` | fn | Normalize a string to NFC (canonical composition). |
| `nfd` | fn | Normalize a string to NFD (canonical decomposition). |
| `nfkc` | fn | Normalize a string to NFKC (compat composition). |
| `nfkd` | fn | Normalize a string to NFKD (compat decomposition). |
| `sentence_count` | fn | Number of UAX #29 sentences in a string. |
| `sentences` | fn | UAX #29 Unicode sentences in a string. |
| `simple_fold` | fn | Next rune in Unicode case-folding cycle. |
| `to_lower` | fn | Simple lowercase mapping for one rune. |
| `to_lower_str` | fn | Full lowercase mapping for a string. |
| `to_title` | fn | Simple titlecase mapping for one rune. |
| `to_upper` | fn | Simple uppercase mapping for one rune. |
| `to_upper_str` | fn | Full uppercase mapping for a string (ss -> SS). |
| `word_bounds` | fn | UAX #29 word boundaries (includes punct + whitespace runs). |
| `word_count` | fn | Number of UAX #29 words in a string. |
| `words` | fn | UAX #29 Unicode words in a string (skips punct/whitespace). |

## `std::utf16`

UTF-16 encoding/decoding and surrogate pair helpers.

| Item | Kind | Doc |
|------|------|-----|
| `decode_surrogate_pair` | fn | Decodes a high+low surrogate pair to a char. |
| `decode_to_string` | fn | Decodes a []u16 to String. |
| `encode_string` | fn | Encodes a String directly to Vec<u16>. |
| `is_surrogate` | fn | True iff r falls in the surrogate range U+D800..U+DFFF. |
| `rune_len` | fn | Number of UTF-16 code units needed to encode r (1 or 2). |

## `std::utf8`

UTF-8 validation and scalar decoding.

| Item | Kind | Doc |
|------|------|-----|
| `append_rune` | fn | Appends the UTF-8 encoding of a rune to a byte Vec. |
| `decode_last_rune` | fn | Decodes the last rune from bytes, returning (rune, width). |
| `decode_last_rune_in_string` | fn | Decodes the last rune from a String, returning (rune, width). |
| `decode_rune` | fn | Decodes the first rune from bytes, returning (rune, width). |
| `decode_rune_in_string` | fn | Decodes the first rune from a String, returning (rune, width). |
| `full_rune` | fn | Reports whether the bytes begin with a full rune. |
| `full_rune_in_string` | fn | Reports whether the String begins with a full rune. |
| `is_valid` | fn | Validates a byte slice as UTF-8. |
| `rune_count` | fn | Counts Unicode scalar values. |
| `rune_count_in_string` | fn | Counts the runes in a String. |
| `rune_len` | fn | Number of bytes needed to encode a rune. |
| `rune_start` | fn | Reports whether the byte could be the first of a rune. |
| `valid_rune` | fn | Reports whether a code point can be legally encoded. |
| `valid_string` | fn | Reports whether a String is valid UTF-8. |

## `std::uuid`

UUID v4 (random) and v7 (timestamp-ordered) generation, parse, and normalize.

| Item | Kind | Doc |
|------|------|-----|
| `is_valid` | fn | Return true iff the string parses as a canonical UUID. |
| `normalize` | fn | Lowercase canonical UUID form of the input, or empty string on parse failure. |
| `simple` | fn | 32-character unhyphenated form of the input, or empty string on parse failure. |
| `v4` | fn | Generate a fresh random v4 UUID as a canonical hyphenated string. |
| `v7` | fn | Generate a fresh v7 (timestamp-ordered) UUID. |

## `std::validate`

Trait-based field validation: implement Validate, collect FieldErrors into Errors.

| Item | Kind | Doc |
|------|------|-----|
| `Errors` | type | Aggregated FieldError set, indexable by dotted path. |
| `FieldError` | type | One field-scoped validation failure: dotted path, message, optional code. |
| `Validate` | trait | Implement on a struct to declare field-level validation rules. |
