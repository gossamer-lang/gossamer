name: Sanitizers

# Audit M1 + M2 (0.6.0). Runs ASan / TSan on the unsafe-touching
# crates against the existing lib test suite. Nightly toolchain
# is required for -Z sanitizer.
#
# Schedule: every push to main + nightly cron (sanitizers are
# slow; running them on every PR is too expensive). On-demand
# invocation via workflow_dispatch when investigating a
# suspected UB regression.

on:
  push:
    branches: [main]
  schedule:
    - cron: "0 5 * * *"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  # See ci.yml for the full explanation. rustup proxies are not
  # concurrent-safe (rustup#988); RUSTUP_TOOLCHAIN suppresses the
  # rust-toolchain.toml override-file read in every proxy
  # invocation, eliminating the rust-src download race. Sanitizers
  # require nightly (`-Z build-std` and `-Z sanitizer`); we pin a
  # specific date so the build is reproducible (`-Z` flags shift
  # across nightlies). `components: rust-src` on the dtolnay step
  # still installs rust-src serially as part of toolchain install;
  # the bypass only affects per-shim consistency checks, not the
  # explicit install. Must match `toolchain:` on the dtolnay step
  # and the `+<toolchain>` flag on every cargo invocation below.
  RUSTUP_TOOLCHAIN: nightly-2026-04-14

jobs:
  asan:
    name: ASan (${{ matrix.os }} nightly)
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: xcode-27
            target: aarch64-apple-darwin
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-04-14
          components: rust-src
      - run: rustup set profile minimal
      - name: Install LLVM (Linux)
        if: runner.os == 'Linux'
        run: |
          set -euo pipefail
          sudo apt-get update
          # `llvm.sh` is the LLVM project's own apt repository installer.
          # Ubuntu's archive stops several majors short of the one this
          # toolchain prefers, and the emitted IR's optimiser has to be
          # the major `rustc` bundles for its bitcode to be readable at
          # all - a mismatch there is a hard link error, not a slower
          # build.
          wget -qO /tmp/llvm.sh https://apt.llvm.org/llvm.sh
          chmod +x /tmp/llvm.sh
          sudo /tmp/llvm.sh 22
          echo "/usr/lib/llvm-22/bin" >> "$GITHUB_PATH"

      - name: Install LLVM (macOS)
        if: runner.os == 'macOS'
        env:
          HOMEBREW_NO_AUTO_UPDATE: "1"
          HOMEBREW_NO_INSTALL_CLEANUP: "1"
        run: |
          set -euo pipefail
          brew install llvm@22
          prefix="$(brew --prefix llvm@22)"
          echo "GOS_LLVM_OPT=$prefix/bin/opt" >> "$GITHUB_ENV"
          echo "GOS_LLC=$prefix/bin/llc"      >> "$GITHUB_ENV"
      - uses: Swatinem/rust-cache@v2
        with:
          prefix-key: v1-asan-${{ matrix.os }}
      # ASan catches the heap-corruption / use-after-free / double-free class
      # the RC migration can regress. Running it on macOS as well as Linux
      # closes the gap where `MALLOC_CHECK_` (glibc-only) gave that coverage on
      # Linux alone - ASan is the portable, stronger equivalent. LeakSanitizer
      # is Linux-only, so `detect_leaks` is left at its platform default. We
      # skip the LLVM-build crates (they shell out to opt/llc, noise unrelated
      # to the unsafe surface we're hardening).
      - name: cargo test under ASan
        env:
          RUSTFLAGS: "-Z sanitizer=address"
          RUSTDOCFLAGS: "-Z sanitizer=address"
          ASAN_OPTIONS: "detect_leaks=0:abort_on_error=1:halt_on_error=1"
          RUST_BACKTRACE: "full"
        run: |
          cargo +nightly-2026-04-14 test \
            -Z build-std \
            --target ${{ matrix.target }} \
            --lib \
            -p gossamer-runtime \
            -p gossamer-interp \
            -p gossamer-coro \
            -p gossamer-mir \
            -p gossamer-binding \
            --no-fail-fast -- --nocapture --test-threads=1

  tsan:
    name: TSan (Linux nightly)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-04-14
          components: rust-src
      - run: rustup set profile minimal
      - name: Install LLVM
        run: |
          set -euo pipefail
          sudo apt-get update
          # `llvm.sh` is the LLVM project's own apt repository installer.
          # Ubuntu's archive stops several majors short of the one this
          # toolchain prefers, and the emitted IR's optimiser has to be
          # the major `rustc` bundles for its bitcode to be readable at
          # all - a mismatch there is a hard link error, not a slower
          # build.
          wget -qO /tmp/llvm.sh https://apt.llvm.org/llvm.sh
          chmod +x /tmp/llvm.sh
          sudo /tmp/llvm.sh 22
          echo "/usr/lib/llvm-22/bin" >> "$GITHUB_PATH"

      - uses: Swatinem/rust-cache@v2
        with:
          prefix-key: v1-tsan
      - name: cargo test under TSan
        env:
          RUSTFLAGS: "-Z sanitizer=thread"
          RUSTDOCFLAGS: "-Z sanitizer=thread"
          TSAN_OPTIONS: "halt_on_error=1:second_deadlock_stack=1"
          RUST_BACKTRACE: "full"
        # TSan-relevant crates: anything that spawns threads or
        # crosses thread boundaries. The MIR / interp lib tests
        # don't exercise concurrency but build cleanly under
        # TSan and surface no false positives.
        run: |
          cargo +nightly-2026-04-14 test \
            -Z build-std \
            --target x86_64-unknown-linux-gnu \
            --lib \
            -p gossamer-runtime \
            -p gossamer-sched \
            -p gossamer-coro \
            --no-fail-fast -- --nocapture --test-threads=1
